# {{Package}} — class diagram

> Generated by `tools/metamodel-gen` — do not edit by hand; re-run the generator.

{{ClassCount}} metaclasses in this package, {{BoundaryCount}} boundary nodes from other packages.

Derived features are omitted. Primitive- and enumeration-typed features are shown inside the class
box; metaclass-typed features are shown as edges (`*--` composition, `-->` association). Boundary
nodes are drawn without their own contents and are never expanded further.

```mermaid
classDiagram
{{#each Classes}}
{{#if HasBody}}
    class {{Name}} {
{{#if IsAbstract}}
        <<abstract>>
{{/if}}
{{#each Attributes}}
        +{{Type}} {{Name}} {{Multiplicity}}
{{/each}}
    }
{{else}}
    class {{Name}}
{{/if}}
{{/each}}
{{#each BoundaryClasses}}
    class {{this}}
{{/each}}
{{#each Relationships}}
    {{Left}} {{{Arrow}}} {{#if Multiplicity}}"{{Multiplicity}}" {{/if}}{{Right}}{{#if Label}} : {{Label}}{{/if}}
{{/each}}
```

## Elements

{{#each Classes}}
- [{{Name}}](../elements/{{Name}}.md)
{{/each}}
