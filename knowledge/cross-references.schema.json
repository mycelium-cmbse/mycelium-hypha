{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mycelium-cmbse/mycelium-hypha/blob/development/knowledge/cross-references.schema.json",
  "title": "Hypha knowledge cross-references",
  "description": "Edges between the three knowledge trees: metamodel element -> spec clause identifier, BNF grammar production and worked example. Generated by tools/spec-extract. Records clause *identifiers* only and never clause text, so it is safe to commit while knowledge/spec stays git-ignored.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "modelVersionUri",
    "documents",
    "provenanceTiers",
    "counts",
    "entries"
  ],
  "properties": {
    "schemaVersion": { "type": "string" },
    "modelVersionUri": { "type": "string" },
    "documents": {
      "description": "Source specifications the clause identifiers refer to, keyed by tree name (kerml, sysml2).",
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["document", "version"],
        "properties": {
          "document": { "type": "string" },
          "version": { "type": "string" }
        }
      }
    },
    "provenanceTiers": {
      "description": "The four provenance tiers and what each means, so a consumer can explain them without hardcoding.",
      "type": "object",
      "additionalProperties": { "type": "string" }
    },
    "counts": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "elements",
        "withClauses",
        "withoutClauses",
        "withGrammar",
        "withFeatures",
        "withExamples",
        "clauseEdges",
        "statedClauseEdges",
        "grammarEdges",
        "featureEdges",
        "exampleEdges"
      ],
      "properties": {
        "elements": { "type": "integer" },
        "withClauses": { "type": "integer" },
        "withoutClauses": { "type": "integer" },
        "withGrammar": { "type": "integer" },
        "withFeatures": { "type": "integer" },
        "withExamples": { "type": "integer" },
        "clauseEdges": { "type": "integer" },
        "statedClauseEdges": {
          "description": "Clause edges the grammar states outright, rather than matched on a clause title.",
          "type": "integer"
        },
        "grammarEdges": { "type": "integer" },
        "featureEdges": { "type": "integer" },
        "exampleEdges": { "type": "integer" }
      }
    },
    "entries": {
      "description": "Keyed by element simple name; every element in knowledge/metamodel/index.json appears, with empty edge arrays where nothing matched.",
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/entry" }
    }
  },
  "$defs": {
    "provenance": {
      "description": "Which tier a fact belongs to. NORMATIVE = verbatim clause-anchored spec text; MODEL = read from the XMI; DERIVED = computed or asserted here.",
      "enum": ["NORMATIVE", "MODEL", "DERIVED"]
    },
    "entry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "element", "clauses", "grammar", "features", "examples"],
      "properties": {
        "kind": { "enum": ["class", "enumeration", "primitiveType"] },
        "element": {
          "description": "Path to the element markdown, relative to the release folder.",
          "type": "string"
        },
        "clauses": { "type": "array", "items": { "$ref": "#/$defs/clauseEdge" } },
        "grammar": { "type": "array", "items": { "$ref": "#/$defs/grammarEdge" } },
        "features": { "type": "array", "items": { "$ref": "#/$defs/featureEdge" } },
        "examples": { "type": "array", "items": { "$ref": "#/$defs/exampleEdge" } }
      }
    },
    "clauseEdge": {
      "description": "The element is treated in this clause. Carries the clause number only - never its title or body.",
      "type": "object",
      "additionalProperties": false,
      "required": ["document", "clause", "provenance", "method"],
      "properties": {
        "document": { "type": "string" },
        "clause": { "type": "string" },
        "provenance": { "$ref": "#/$defs/provenance" },
        "method": { "type": "string" }
      }
    },
    "grammarEdge": {
      "type": "object",
      "additionalProperties": false,
      "required": ["grammar", "production", "provenance", "method"],
      "properties": {
        "grammar": { "type": "string" },
        "production": { "type": "string" },
        "provenance": { "$ref": "#/$defs/provenance" },
        "method": { "type": "string" }
      }
    },
    "featureEdge": {
      "description": "A metamodel feature that this element's grammar productions populate, and how: '=' sets a value, '+=' adds to a collection, '?=' sets a boolean flag from a keyword's presence. Stated by the grammar, so MODEL rather than DERIVED.",
      "type": "object",
      "additionalProperties": false,
      "required": ["grammar", "feature", "operator", "productions", "provenance", "method"],
      "properties": {
        "grammar": { "type": "string" },
        "feature": { "type": "string" },
        "operator": { "enum": ["=", "+=", "?="] },
        "productions": { "type": "array", "items": { "type": "string" } },
        "provenance": { "$ref": "#/$defs/provenance" },
        "method": { "type": "string" }
      }
    },
    "exampleEdge": {
      "type": "object",
      "additionalProperties": false,
      "required": ["file", "provenance", "method"],
      "properties": {
        "file": {
          "description": "Path to the worked example, relative to knowledge/.",
          "type": "string"
        },
        "provenance": { "$ref": "#/$defs/provenance" },
        "method": { "type": "string" }
      }
    }
  }
}
