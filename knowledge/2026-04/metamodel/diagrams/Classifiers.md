# Classifiers — class diagram

> Generated by `tools/metamodel-gen` — do not edit by hand; re-run the generator.

2 metaclasses in this package, 2 boundary nodes from other packages.

Derived features are omitted. Primitive- and enumeration-typed features are shown inside the class
box; metaclass-typed features are shown as edges (`*--` composition, `-->` association). Boundary
nodes are drawn without their own contents and are never expanded further.

```mermaid
classDiagram
    class Classifier
    class Subclassification
    class Specialization
    class Type
    Specialization <|-- Subclassification
    Subclassification --> "1" Classifier : subclassifier
    Subclassification --> "1" Classifier : superclassifier
    Type <|-- Classifier
```

## Elements

- [Classifier](../elements/Classifier.md)
- [Subclassification](../elements/Subclassification.md)
