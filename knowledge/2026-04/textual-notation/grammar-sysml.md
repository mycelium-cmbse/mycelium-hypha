---
grammar: SysML
tag: 2026-04
kind: grammar-reference
productions: 350
---

# SysML textual grammar — 2026-04

> Generated by `tools/spec-extract` from the committed `.kebnf` (EPL-2.0) — do not edit by
> hand; re-run the generator.

Each production shows the metaclass it builds (`produces`), the specification clause it is
defined in, and the metamodel features it populates (`=` sets, `+=` adds, `?=` is a boolean
flag). Both come from the grammar itself, not from name matching.

## 8.2.2.1.2 Lexical Structure

### RESERVED_KEYWORD

clause `8.2.2.1.2`

```kebnf
RESERVED_KEYWORD =
    'about' | 'abstract' | 'accept' | 'action' | 'actor' | 'after' | 'alias' | 'all' | 'allocate' | 'allocation'
    | 'analysis' | 'and' | 'as' | 'assert' | 'assign' | 'assume' | 'at' | 'attribute' | 'bind' | 'binding' | 'by' | 'calc'
    | 'case' | 'comment' | 'concern' | 'connect' | 'connection' | 'constant' | 'constraint' | 'crosses' | 'decide'
    | 'def' | 'default' | 'defined' | 'dependency' | 'derived' | 'do' | 'doc' | 'else' | 'end' | 'entry' | 'enum'
    | 'event' | 'exhibit' | 'exit' | 'expose' | 'false' | 'filter' | 'first' | 'flow' | 'for' | 'fork' | 'frame' | 'from'
    | 'hastype' | 'if' | 'implies' | 'import' | 'in' | 'include' | 'individual' | 'inout' | 'interface' | 'istype'
    | 'item' | 'join' | 'language' | 'library' | 'locale' | 'loop' | 'merge' | 'message' | 'meta' | 'metadata'
    | 'nonunique' | 'not' | 'null' | 'objective' | 'occurrence' | 'of' | 'or' | 'ordered' | 'out' | 'package' | 'parallel'
    | 'part' | 'perform' | 'port' | 'private' | 'protected' | 'public' | 'redefines' | 'ref' | 'references' | 'render'
    | 'rendering' | 'rep' | 'require' | 'requirement' | 'return' | 'satisfy' | 'send' | 'snapshot' | 'specializes'
    | 'stakeholder' | 'standard' | 'state' | 'subject' | 'subsets' | 'succession' | 'terminate' | 'then' | 'timeslice'
    | 'to' | 'transition' | 'true' | 'until' | 'use' | 'variant' | 'variation' | 'verification' | 'verify' | 'via'
    | 'view' | 'viewpoint' | 'when' | 'while' | 'xor'
```

### DEFINED_BY

clause `8.2.2.1.2`

```kebnf
DEFINED_BY  = ':'   | 'defined' 'by'
```

### SPECIALIZES

clause `8.2.2.1.2`

```kebnf
SPECIALIZES = ':>'  | 'specializes'
```

### SUBSETS

clause `8.2.2.1.2`

```kebnf
SUBSETS     = ':>'  | 'subsets'
```

### REFERENCES

clause `8.2.2.1.2`

```kebnf
REFERENCES  = '::>' | 'references'
```

### CROSSES

clause `8.2.2.1.2`

```kebnf
CROSSES     = '=>'  | 'crosses'
```

### REDEFINES

clause `8.2.2.1.2`

```kebnf
REDEFINES   = ':>>' | 'redefines'
```

## 8.2.2.2 Elements and Relationships Textual Notation

### Identification

produces [Element](../metamodel/elements/Element.md) · clause `8.2.2.2` · features `declaredName =`, `declaredShortName =`

```kebnf
Identification : Element =
    ( '<' declaredShortName = NAME '>' )?
    ( declaredName = NAME )?
```

### RelationshipBody

produces [Relationship](../metamodel/elements/Relationship.md) · clause `8.2.2.2` · features `ownedRelationship +=`

```kebnf
RelationshipBody : Relationship =
    ';' | '{' ( ownedRelationship += OwnedAnnotation )* '}'
```

## 8.2.2.3 Dependencies Textual Notation

### Dependency

clause `8.2.2.3` · features `ownedRelationship +=`

```kebnf
Dependency =
    ( ownedRelationship += PrefixMetadataAnnotation )*
    'dependency' DependencyDeclaration
    RelationshipBody
```

### DependencyDeclaration

clause `8.2.2.3` · features `client +=`, `supplier +=`

```kebnf
DependencyDeclaration =
    ( Identification 'from' )?
    client += [QualifiedName] ( ',' client += [QualifiedName] )* 'to'
    supplier += [QualifiedName] ( ',' supplier += [QualifiedName] )*
```

## 8.2.2.4.1 Annotations

### Annotation

clause `8.2.2.4.1` · features `annotatedElement =`

```kebnf
Annotation =
    annotatedElement = [QualifiedName]
```

### OwnedAnnotation

produces [Annotation](../metamodel/elements/Annotation.md) · clause `8.2.2.4.1` · features `ownedRelatedElement +=`

```kebnf
OwnedAnnotation : Annotation =
    ownedRelatedElement += AnnotatingElement
```

### AnnotatingMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.4.1` · features `ownedRelatedElement +=`

```kebnf
AnnotatingMember : OwningMembership =
    ownedRelatedElement += AnnotatingElement
```

### AnnotatingElement

clause `8.2.2.4.1`

```kebnf
AnnotatingElement =
      Comment
    | Documentation
    | TextualRepresentation
    | MetadataFeature
```

## 8.2.2.4.2 Comments and Documentation

### Comment

clause `8.2.2.4.2` · features `body =`, `locale =`, `ownedRelationship +=`

```kebnf
Comment =
    ( 'comment' Identification
      ( 'about' ownedRelationship += Annotation
        ( ',' ownedRelationship += Annotation )*
      )?
    )?
    ( 'locale' locale = STRING_VALUE )?
    body = REGULAR_COMMENT
```

### Documentation

clause `8.2.2.4.2` · features `body =`, `locale =`

```kebnf
Documentation =
    'doc' Identification
    ( 'locale' locale = STRING_VALUE )?
    body = REGULAR_COMMENT
```

## 8.2.2.4.3 Textual Representation

### TextualRepresentation

clause `8.2.2.4.3` · features `body =`, `language =`

```kebnf
TextualRepresentation =
    ( 'rep' Identification )?
    'language' language = STRING_VALUE body = REGULAR_COMMENT
```

## 8.2.2.5.1 Packages

### RootNamespace

produces [Namespace](../metamodel/elements/Namespace.md) · clause `8.2.2.5.1`

```kebnf
RootNamespace : Namespace =
    PackageBodyElement*
```

### Package

clause `8.2.2.5.1` · features `ownedRelationship +=`

```kebnf
Package =
    ( ownedRelationship += PrefixMetadataMember )*
    PackageDeclaration PackageBody
```

### LibraryPackage

clause `8.2.2.5.1` · features `isStandard ?=`, `ownedRelationship +=`

```kebnf
LibraryPackage =
    ( isStandard ?= 'standard' ) 'library'
    ( ownedRelationship += PrefixMetadataMember )*
    PackageDeclaration PackageBody
```

### PackageDeclaration

produces [Package](../metamodel/elements/Package.md) · clause `8.2.2.5.1`

```kebnf
PackageDeclaration : Package =
    'package' Identification
```

### PackageBody

produces [Package](../metamodel/elements/Package.md) · clause `8.2.2.5.1`

```kebnf
PackageBody : Package =
    ';' | '{' PackageBodyElement* '}'
```

### PackageBodyElement

produces [Package](../metamodel/elements/Package.md) · clause `8.2.2.5.1` · features `ownedRelationship +=`

```kebnf
PackageBodyElement : Package =
      ownedRelationship += PackageMember
    | ownedRelationship += ElementFilterMember
    | ownedRelationship += AliasMember
    | ownedRelationship += Import
```

### MemberPrefix

produces [Membership](../metamodel/elements/Membership.md) · clause `8.2.2.5.1` · features `visibility =`

```kebnf
MemberPrefix : Membership =
    ( visibility = VisibilityIndicator )?
```

### PackageMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.5.1` · features `ownedRelatedElement +=`, `ownedRelatedElement =`

```kebnf
PackageMember : OwningMembership =
    MemberPrefix
    ( ownedRelatedElement += DefinitionElement
    | ownedRelatedElement = UsageElement )
```

### ElementFilterMember

produces [ElementFilterMembership](../metamodel/elements/ElementFilterMembership.md) · clause `8.2.2.5.1` · features `ownedRelatedElement +=`

```kebnf
ElementFilterMember : ElementFilterMembership =
    MemberPrefix
    'filter' ownedRelatedElement += OwnedExpression ';'
```

### AliasMember

produces [Membership](../metamodel/elements/Membership.md) · clause `8.2.2.5.1` · features `memberElement =`, `memberName =`, `memberShortName =`

```kebnf
AliasMember : Membership =
    MemberPrefix
    'alias' ( '<' memberShortName = NAME '>' )?
    ( memberName = NAME )?
     'for' memberElement = [QualifiedName]
    RelationshipBody
```

### Import

clause `8.2.2.5.1` · features `isImportAll ?=`, `visibility =`

```kebnf
Import =
    visibility = VisibilityIndicator
    'import' ( isImportAll ?= 'all' )?
    ImportDeclaration
    RelationshipBody
```

### ImportDeclaration

produces [Import](../metamodel/elements/Import.md) · clause `8.2.2.5.1`

```kebnf
ImportDeclaration : Import =
    MembershipImport | NamespaceImport
```

### MembershipImport

clause `8.2.2.5.1` · features `importedMembership =`, `isRecursive ?=`

```kebnf
MembershipImport =
    importedMembership = [QualifiedName]
    ( '::' isRecursive ?= '**' )?
```

### NamespaceImport

clause `8.2.2.5.1` · features `importedNamespace =`, `isRecursive ?=`, `ownedRelatedElement +=`

```kebnf
NamespaceImport =
      importedNamespace = [QualifiedName] '::' '*'
      ( '::' isRecursive ?= '**' )?
    | importedNamespace = FilterPackage
      { ownedRelatedElement += importedNamespace }
```

### FilterPackage

produces [Package](../metamodel/elements/Package.md) · clause `8.2.2.5.1` · features `ownedRelationship +=`

```kebnf
FilterPackage : Package =
    ownedRelationship += FilterPackageImport
    ( ownedRelationship += FilterPackageMember )+
```

### FilterPackageMember

produces [ElementFilterMembership](../metamodel/elements/ElementFilterMembership.md) · clause `8.2.2.5.1` · features `ownedRelatedElement +=`

```kebnf
FilterPackageMember : ElementFilterMembership =
    '[' ownedRelatedElement += OwnedExpression ']'
```

### VisibilityIndicator

produces [VisibilityKind](../metamodel/elements/VisibilityKind.md) · clause `8.2.2.5.1`

```kebnf
VisibilityIndicator : VisibilityKind =
    'public' | 'private' | 'protected'
```

## 8.2.2.5.2 Package Elements

### DefinitionElement

produces [Element](../metamodel/elements/Element.md) · clause `8.2.2.5.2`

```kebnf
DefinitionElement : Element =
      Package
    | LibraryPackage
    | AnnotatingElement
    | Dependency
    | AttributeDefinition
    | EnumerationDefinition
    | OccurrenceDefinition
    | IndividualDefinition
    | ItemDefinition
    | PartDefinition
    | ConnectionDefinition
    | FlowDefinition
    | InterfaceDefinition
    | PortDefinition
    | ActionDefinition
    | CalculationDefinition
    | StateDefinition
    | ConstraintDefinition
    | RequirementDefinition
    | ConcernDefinition
    | CaseDefinition
    | AnalysisCaseDefinition
    | VerificationCaseDefinition
    | UseCaseDefinition
    | ViewDefinition
    | ViewpointDefinition
    | RenderingDefinition
    | MetadataDefinition
    | ExtendedDefinition
```

### UsageElement

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.5.2`

```kebnf
UsageElement : Usage =
      NonOccurrenceUsageElement
    | OccurrenceUsageElement
```

## 8.2.2.6.1 Definitions

### BasicDefinitionPrefix

clause `8.2.2.6.1` · features `isAbstract ?=`, `isVariation ?=`

```kebnf
BasicDefinitionPrefix =
    isAbstract ?= 'abstract' | isVariation ?= 'variation'
```

### DefinitionExtensionKeyword

produces [Definition](../metamodel/elements/Definition.md) · clause `8.2.2.6.1` · features `ownedRelationship +=`

```kebnf
DefinitionExtensionKeyword : Definition =
    ownedRelationship += PrefixMetadataMember
```

### DefinitionPrefix

produces [Definition](../metamodel/elements/Definition.md) · clause `8.2.2.6.1`

```kebnf
DefinitionPrefix : Definition =
    BasicDefinitionPrefix? DefinitionExtensionKeyword*
```

### Definition

clause `8.2.2.6.1`

```kebnf
Definition =
    DefinitionDeclaration DefinitionBody
```

### DefinitionDeclaration

produces [Definition](../metamodel/elements/Definition.md) · clause `8.2.2.6.1`

```kebnf
DefinitionDeclaration : Definition =
    Identification SubclassificationPart?
```

### DefinitionBody

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.6.1`

```kebnf
DefinitionBody : Type =
      ';' | '{' DefinitionBodyItem* '}'
```

### DefinitionBodyItem

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.6.1` · features `ownedRelationship +=`

```kebnf
DefinitionBodyItem : Type =
      ownedRelationship += DefinitionMember
    | ownedRelationship += VariantUsageMember
    | ownedRelationship += NonOccurrenceUsageMember
    | ( ownedRelationship += SourceSuccessionMember )?
      ownedRelationship += OccurrenceUsageMember
    | ownedRelationship += AliasMember
    | ownedRelationship += Import
```

### DefinitionMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.6.1` · features `ownedRelatedElement +=`

```kebnf
DefinitionMember : OwningMembership =
    MemberPrefix
    ownedRelatedElement += DefinitionElement
```

### VariantUsageMember

produces [VariantMembership](../metamodel/elements/VariantMembership.md) · clause `8.2.2.6.1` · features `ownedVariantUsage =`

```kebnf
VariantUsageMember : VariantMembership =
    MemberPrefix 'variant'
    ownedVariantUsage = VariantUsageElement
```

### NonOccurrenceUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.6.1` · features `ownedRelatedElement +=`

```kebnf
NonOccurrenceUsageMember : FeatureMembership =
    MemberPrefix
    ownedRelatedElement += NonOccurrenceUsageElement
```

### OccurrenceUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.6.1` · features `ownedRelatedElement +=`

```kebnf
OccurrenceUsageMember : FeatureMembership =
    MemberPrefix
    ownedRelatedElement += OccurrenceUsageElement
```

### StructureUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.6.1` · features `ownedRelatedElement +=`

```kebnf
StructureUsageMember : FeatureMembership =
    MemberPrefix
    ownedRelatedElement += StructureUsageElement
```

### BehaviorUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.6.1` · features `ownedRelatedElement +=`

```kebnf
BehaviorUsageMember : FeatureMembership =
    MemberPrefix
    ownedRelatedElement += BehaviorUsageElement
```

## 8.2.2.6.2 Usages

### FeatureDirection

produces [FeatureDirectionKind](../metamodel/elements/FeatureDirectionKind.md) · clause `8.2.2.6.2`

```kebnf
FeatureDirection : FeatureDirectionKind =
    'in' | 'out' | 'inout'
```

### RefPrefix

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2` · features `direction =`, `isAbstract ?=`, `isConstant ?=`, `isDerived ?=`, `isVariation ?=`

```kebnf
RefPrefix : Usage =
    ( direction = FeatureDirection )?
    ( isDerived ?= 'derived' )?
    ( isAbstract ?= 'abstract' | isVariation ?= 'variation' )?
    ( isConstant ?= 'constant' )?
```

### BasicUsagePrefix

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2` · features `isReference ?=`

```kebnf
BasicUsagePrefix : Usage =
    RefPrefix
    ( isReference ?= 'ref' )?
```

### EndUsagePrefix

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2` · features `isEnd ?=`, `ownedRelationship +=`

```kebnf
EndUsagePrefix : Usage =
    isEnd ?= 'end' ( ownedRelationship += OwnedCrossFeatureMember )?

// (See Note 1)
```

### OwnedCrossFeatureMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.6.2` · features `ownedRelatedElement +=`

```kebnf
OwnedCrossFeatureMember : OwningMembership =
    ownedRelatedElement += OwnedCrossFeature
```

### OwnedCrossFeature

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.6.2`

```kebnf
OwnedCrossFeature : ReferenceUsage =
    BasicUsagePrefix UsageDeclaration
```

### UsageExtensionKeyword

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2` · features `ownedRelationship +=`

```kebnf
UsageExtensionKeyword : Usage =
    ownedRelationship += PrefixMetadataMember
```

### UnextendedUsagePrefix

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2`

```kebnf
UnextendedUsagePrefix : Usage =
    EndUsagePrefix | BasicUsagePrefix
```

### UsagePrefix

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2`

```kebnf
UsagePrefix : Usage =
    UnextendedUsagePrefix UsageExtensionKeyword*
```

### Usage

clause `8.2.2.6.2`

```kebnf
Usage =
    UsageDeclaration UsageCompletion
```

### UsageDeclaration

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2`

```kebnf
UsageDeclaration : Usage =
    Identification FeatureSpecializationPart?
```

### UsageCompletion

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2`

```kebnf
UsageCompletion : Usage =
    ValuePart? UsageBody
```

### UsageBody

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.2`

```kebnf
UsageBody : Usage =
    DefinitionBody
```

### ValuePart

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.2` · features `ownedRelationship +=`

```kebnf
ValuePart : Feature =
    ownedRelationship += FeatureValue
```

### FeatureValue

clause `8.2.2.6.2` · features `isDefault ?=`, `isEnd =`, `isInitial ?=`, `mayTimeVary =`, `ownedRelatedElement +=`

```kebnf
FeatureValue =
    ( '='
    | isInitial ?= ':='
    | isDefault ?= 'default' ( '=' | isInitial ?= ':=' )?
    )
    ownedRelatedElement += OwnedExpression

// Notes:
//   1. A Usage parsed with isEnd = true for which mayTimeVary = true must also have isConstant set to true, even though this is not explicitly notated in the textual notation, in order to satisfy the KerML constraint checkFeatureEndIsConstant.
```

## 8.2.2.6.3 Reference Usages

### DefaultReferenceUsage

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.6.3`

```kebnf
DefaultReferenceUsage : ReferenceUsage =
    RefPrefix Usage
```

### ReferenceUsage

clause `8.2.2.6.3`

```kebnf
ReferenceUsage =
    ( EndUsagePrefix | RefPrefix )
    'ref' Usage
```

### VariantReference

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.6.3` · features `ownedRelationship +=`

```kebnf
VariantReference : ReferenceUsage =
    ownedRelationship += OwnedReferenceSubsetting
    FeatureSpecialization* UsageBody
```

## 8.2.2.6.4 Body Elements

### NonOccurrenceUsageElement

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.4`

```kebnf
NonOccurrenceUsageElement : Usage =
      DefaultReferenceUsage
    | ReferenceUsage
    | AttributeUsage
    | EnumerationUsage
    | BindingConnectorAsUsage
    | SuccessionAsUsage
    | ExtendedUsage
```

### OccurrenceUsageElement

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.4`

```kebnf
OccurrenceUsageElement : Usage =
    StructureUsageElement | BehaviorUsageElement
```

### StructureUsageElement

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.4`

```kebnf
StructureUsageElement : Usage =
      OccurrenceUsage
    | IndividualUsage
    | PortionUsage
    | EventOccurrenceUsage
    | ItemUsage
    | PartUsage
    | ViewUsage
    | RenderingUsage
    | PortUsage
    | ConnectionUsage
    | InterfaceUsage
    | AllocationUsage
    | Message
    | FlowUsage
    | SuccessionFlowUsage
```

### BehaviorUsageElement

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.4`

```kebnf
BehaviorUsageElement : Usage =
      ActionUsage
    | CalculationUsage
    | StateUsage
    | ConstraintUsage
    | RequirementUsage
    | ConcernUsage
    | CaseUsage
    | AnalysisCaseUsage
    | VerificationCaseUsage
    | UseCaseUsage
    | ViewpointUsage
    | PerformActionUsage
    | ExhibitStateUsage
    | IncludeUseCaseUsage
    | AssertConstraintUsage
    | SatisfyRequirementUsage
```

### VariantUsageElement

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.6.4`

```kebnf
VariantUsageElement : Usage =
      VariantReference
    | ReferenceUsage
    | AttributeUsage
    | BindingConnectorAsUsage
    | SuccessionAsUsage
    | OccurrenceUsage
    | IndividualUsage
    | PortionUsage
    | EventOccurrenceUsage
    | ItemUsage
    | PartUsage
    | ViewUsage
    | RenderingUsage
    | PortUsage
    | ConnectionUsage
    | InterfaceUsage
    | AllocationUsage
    | Message
    | FlowUsage
    | SuccessionFlowUsage
    | BehaviorUsageElement
```

## 8.2.2.6.5 Specialization

### SubclassificationPart

produces [Classifier](../metamodel/elements/Classifier.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
SubclassificationPart : Classifier =
      SPECIALIZES ownedRelationship += OwnedSubclassification
      ( ',' ownedRelationship += OwnedSubclassification )*
```

### OwnedSubclassification

produces [Subclassification](../metamodel/elements/Subclassification.md) · clause `8.2.2.6.5` · features `superClassifier =`

```kebnf
OwnedSubclassification : Subclassification =
    superClassifier = [QualifiedName]
```

### FeatureSpecializationPart

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5`

```kebnf
FeatureSpecializationPart : Feature =
      FeatureSpecialization+ MultiplicityPart? FeatureSpecialization*
    | MultiplicityPart FeatureSpecialization*
```

### FeatureSpecialization

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5`

```kebnf
FeatureSpecialization : Feature =
    Typings | Subsettings | References | Crosses | Redefinitions
```

### Typings

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
Typings : Feature =
      TypedBy ( ',' ownedRelationship += FeatureTyping )*
```

### TypedBy

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
TypedBy : Feature =
    DEFINED_BY ownedRelationship += FeatureTyping
```

### FeatureTyping

clause `8.2.2.6.5`

```kebnf
FeatureTyping =
    OwnedFeatureTyping | ConjugatedPortTyping
```

### OwnedFeatureTyping

produces [FeatureTyping](../metamodel/elements/FeatureTyping.md) · clause `8.2.2.6.5` · features `ownedRelatedElement +=`, `type =`

```kebnf
OwnedFeatureTyping : FeatureTyping =
      type = [QualifiedName]
    | type = OwnedFeatureChain
      { ownedRelatedElement += type }
```

### Subsettings

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
Subsettings : Feature =
    Subsets ( ',' ownedRelationship += OwnedSubsetting )*
```

### Subsets

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
Subsets : Feature =
    SUBSETS ownedRelationship += OwnedSubsetting
```

### OwnedSubsetting

produces [Subsetting](../metamodel/elements/Subsetting.md) · clause `8.2.2.6.5` · features `ownedRelatedElement +=`, `subsettedFeature =`

```kebnf
OwnedSubsetting : Subsetting =
      subsettedFeature = [QualifiedName]
    | subsettedFeature = OwnedFeatureChain
      { ownedRelatedElement += subsettedFeature }
```

### References

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
References : Feature =
    REFERENCES ownedRelationship += OwnedReferenceSubsetting
```

### OwnedReferenceSubsetting

produces [ReferenceSubsetting](../metamodel/elements/ReferenceSubsetting.md) · clause `8.2.2.6.5` · features `ownedRelatedElement +=`, `referencedFeature =`

```kebnf
OwnedReferenceSubsetting : ReferenceSubsetting =
      referencedFeature = [QualifiedName]
    | referencedFeature = OwnedFeatureChain
      { ownedRelatedElement += referenceFeature }
```

### Crosses

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
Crosses : Feature =
    CROSSES ownedRelationship += OwnedCrossSubsetting
```

### OwnedCrossSubsetting

produces [CrossSubsetting](../metamodel/elements/CrossSubsetting.md) · clause `8.2.2.6.5` · features `crossedFeature =`, `ownedRelatedElement +=`

```kebnf
OwnedCrossSubsetting : CrossSubsetting =
      crossedFeature = [QualifiedName]
    | crossedFeature = OwnedFeatureChain
      { ownedRelatedElement += crossedFeature }
```

### Redefinitions

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
Redefinitions : Feature =
    Redefines ( ',' ownedRelationship += OwnedRedefinition )*
```

### Redefines

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
Redefines : Feature =
    REDEFINES ownedRelationship += OwnedRedefinition
```

### OwnedRedefinition

produces [Redefinition](../metamodel/elements/Redefinition.md) · clause `8.2.2.6.5` · features `ownedRelatedElement +=`, `redefinedFeature =`

```kebnf
OwnedRedefinition : Redefinition =
      redefinedFeature = [QualifiedName]
    | redefinedFeature = OwnedFeatureChain
      { ownedRelatedElement += redefinedFeature }
```

### OwnedFeatureChain

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.5` · features `ownedRelationship +=`

```kebnf
OwnedFeatureChain : Feature =
    ownedRelationship += OwnedFeatureChaining
    ( '.' ownedRelationship += OwnedFeatureChaining )+
```

### OwnedFeatureChaining

produces [FeatureChaining](../metamodel/elements/FeatureChaining.md) · clause `8.2.2.6.5` · features `chainingFeature =`

```kebnf
OwnedFeatureChaining : FeatureChaining =
    chainingFeature = [QualifiedName]
```

## 8.2.2.6.6 Multiplicity

### MultiplicityPart

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.6.6` · features `isOrdered ?=`, `isUnique =`, `ownedRelationship +=`

```kebnf
MultiplicityPart : Feature =
      ownedRelationship += OwnedMultiplicity
    | ( ownedRelationship += OwnedMultiplicity )?
      ( isOrdered ?= 'ordered' ( { isUnique = false } 'nonunique' )?
      | { isUnique = false } 'nonunique' ( isOrdered ?= 'ordered' )? )
```

### OwnedMultiplicity

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.6.6` · features `ownedRelatedElement +=`

```kebnf
OwnedMultiplicity : OwningMembership =
    ownedRelatedElement += MultiplicityRange
```

### MultiplicityRange

clause `8.2.2.6.6` · features `ownedRelationship +=`

```kebnf
MultiplicityRange =
    '[' ( ownedRelationship += MultiplicityExpressionMember '..' )?
          ownedRelationship += MultiplicityExpressionMember ']'
```

### MultiplicityExpressionMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.6.6` · features `ownedRelatedElement +=`

```kebnf
MultiplicityExpressionMember : OwningMembership =
    ownedRelatedElement += ( LiteralExpression | FeatureReferenceExpression )
```

## 8.2.2.7 Attributes Textual Notation

### AttributeDefinition

produces [AttributeDefinition](../metamodel/elements/AttributeDefinition.md) · clause `8.2.2.7`

```kebnf
AttributeDefinition : AttributeDefinition =
    DefinitionPrefix 'attribute' 'def' Definition
```

### AttributeUsage

produces [AttributeUsage](../metamodel/elements/AttributeUsage.md) · clause `8.2.2.7`

```kebnf
AttributeUsage : AttributeUsage =
    UsagePrefix 'attribute' Usage
```

## 8.2.2.8 Enumerations Textual Notation

### EnumerationDefinition

clause `8.2.2.8`

```kebnf
EnumerationDefinition =
    DefinitionExtensionKeyword*
    'enum' 'def' DefinitionDeclaration EnumerationBody
```

### EnumerationBody

produces [EnumerationDefinition](../metamodel/elements/EnumerationDefinition.md) · clause `8.2.2.8` · features `ownedRelationship +=`

```kebnf
EnumerationBody : EnumerationDefinition =
      ';'
    | '{' ( ownedRelationship += AnnotatingMember
          | ownedRelationship += EnumerationUsageMember )*
      '}'
```

### EnumerationUsageMember

produces [VariantMembership](../metamodel/elements/VariantMembership.md) · clause `8.2.2.8` · features `ownedRelatedElement +=`

```kebnf
EnumerationUsageMember : VariantMembership =
    MemberPrefix ownedRelatedElement += EnumeratedValue
```

### EnumeratedValue

produces [EnumerationUsage](../metamodel/elements/EnumerationUsage.md) · clause `8.2.2.8`

```kebnf
EnumeratedValue : EnumerationUsage =
    'enum'? Usage
```

### EnumerationUsage

produces [EnumerationUsage](../metamodel/elements/EnumerationUsage.md) · clause `8.2.2.8`

```kebnf
EnumerationUsage : EnumerationUsage =
    UsagePrefix 'enum' Usage
```

## 8.2.2.9.1 Occurrence Definitions

### OccurrenceDefinitionPrefix

produces [OccurrenceDefinition](../metamodel/elements/OccurrenceDefinition.md) · clause `8.2.2.9.1` · features `isIndividual ?=`, `ownedRelationship +=`

```kebnf
OccurrenceDefinitionPrefix : OccurrenceDefinition =
    BasicDefinitionPrefix?
    ( isIndividual ?= 'individual'
      ownedRelationship += EmptyMultiplicityMember
    )?
    DefinitionExtensionKeyword*
```

### OccurrenceDefinition

clause `8.2.2.9.1`

```kebnf
OccurrenceDefinition =
    OccurrenceDefinitionPrefix 'occurrence' 'def' Definition
```

### IndividualDefinition

produces [OccurrenceDefinition](../metamodel/elements/OccurrenceDefinition.md) · clause `8.2.2.9.1` · features `isIndividual ?=`, `ownedRelationship +=`

```kebnf
IndividualDefinition : OccurrenceDefinition =
    BasicDefinitionPrefix? isIndividual ?= 'individual'
    DefinitionExtensionKeyword* 'def' Definition
    ownedRelationship += EmptyMultiplicityMember
```

### EmptyMultiplicityMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.9.1` · features `ownedRelatedElement +=`

```kebnf
EmptyMultiplicityMember : OwningMembership =
    ownedRelatedElement += EmptyMultiplicity
```

### EmptyMultiplicity

produces [Multiplicity](../metamodel/elements/Multiplicity.md) · clause `8.2.2.9.1`

```kebnf
EmptyMultiplicity : Multiplicity =
    { }
```

## 8.2.2.9.2 Occurrence Usages

### OccurrenceUsagePrefix

produces [OccurrenceUsage](../metamodel/elements/OccurrenceUsage.md) · clause `8.2.2.9.2` · features `isIndividual ?=`, `isPortion =`, `portionKind =`

```kebnf
OccurrenceUsagePrefix : OccurrenceUsage =
    BasicUsagePrefix
    ( isIndividual ?= 'individual' )?
    ( portionKind = PortionKind
      { isPortion = true }
    )?
    UsageExtensionKeyword*
```

### OccurrenceUsage

clause `8.2.2.9.2`

```kebnf
OccurrenceUsage =
    OccurrenceUsagePrefix 'occurrence' Usage
```

### IndividualUsage

produces [OccurrenceUsage](../metamodel/elements/OccurrenceUsage.md) · clause `8.2.2.9.2` · features `isIndividual ?=`

```kebnf
IndividualUsage : OccurrenceUsage =
    BasicUsagePrefix isIndividual ?= 'individual'
    UsageExtensionKeyword* Usage
```

### PortionUsage

produces [OccurrenceUsage](../metamodel/elements/OccurrenceUsage.md) · clause `8.2.2.9.2` · features `isIndividual ?=`, `isPortion =`, `portionKind =`

```kebnf
PortionUsage : OccurrenceUsage  =
    BasicUsagePrefix ( isIndividual ?= 'individual' )?
    portionKind = PortionKind
    UsageExtensionKeyword* Usage
    { isPortion = true }
```

### PortionKind

clause `8.2.2.9.2`

```kebnf
PortionKind =
    'snapshot' | 'timeslice'
```

### EventOccurrenceUsage

clause `8.2.2.9.2` · features `ownedRelationship +=`

```kebnf
EventOccurrenceUsage =
    OccurrenceUsagePrefix 'event'
    ( ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart?
    | 'occurrence' UsageDeclaration? )
    UsageCompletion
```

## 8.2.2.9.3 Occurrence Successions

### SourceSuccessionMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.9.3` · features `ownedRelatedElement +=`

```kebnf
SourceSuccessionMember  : FeatureMembership =
    'then' ownedRelatedElement += SourceSuccession
```

### SourceSuccession

produces [SuccessionAsUsage](../metamodel/elements/SuccessionAsUsage.md) · clause `8.2.2.9.3` · features `ownedRelationship +=`

```kebnf
SourceSuccession : SuccessionAsUsage =
    ownedRelationship += SourceEndMember
```

### SourceEndMember

produces [EndFeatureMembership](../metamodel/elements/EndFeatureMembership.md) · clause `8.2.2.9.3` · features `ownedRelatedElement +=`

```kebnf
SourceEndMember : EndFeatureMembership =
    ownedRelatedElement += SourceEnd
```

### SourceEnd

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.9.3` · features `ownedRelationship +=`

```kebnf
SourceEnd : ReferenceUsage =
    ( ownedRelationship += OwnedMultiplicity )?
```

## 8.2.2.10 Items Textual Notation

### ItemDefinition

clause `8.2.2.10`

```kebnf
ItemDefinition =
    OccurrenceDefinitionPrefix
    'item' 'def' Definition
```

### ItemUsage

clause `8.2.2.10`

```kebnf
ItemUsage =
    OccurrenceUsagePrefix 'item' Usage
```

## 8.2.2.11 Parts Textual Notation

### PartDefinition

clause `8.2.2.11`

```kebnf
PartDefinition =
    OccurrenceDefinitionPrefix 'part' 'def' Definition
```

### PartUsage

clause `8.2.2.11`

```kebnf
PartUsage =
    OccurrenceUsagePrefix 'part' Usage
```

## 8.2.2.12 Ports Textual Notation

### PortDefinition

clause `8.2.2.12` · features `originalPortDefinition =`, `ownedRelationship +=`

```kebnf
PortDefinition =
    DefinitionPrefix 'port' 'def' Definition
    ownedRelationship += ConjugatedPortDefinitionMember
    { conjugatedPortDefinition.ownedPortConjugator.
        originalPortDefinition = this }

// (See Note 1)
```

### ConjugatedPortDefinitionMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.12` · features `ownedRelatedElement +=`

```kebnf
ConjugatedPortDefinitionMember : OwningMembership =
    ownedRelatedElement += ConjugatedPortDefinition
```

### ConjugatedPortDefinition

clause `8.2.2.12` · features `ownedRelationship +=`

```kebnf
ConjugatedPortDefinition =
    ownedRelationship += PortConjugation
```

### PortConjugation

clause `8.2.2.12`

```kebnf
PortConjugation =
    {}
```

### PortUsage

clause `8.2.2.12`

```kebnf
PortUsage =
    OccurrenceUsagePrefix 'port' Usage
```

### ConjugatedPortTyping

produces [ConjugatedPortTyping](../metamodel/elements/ConjugatedPortTyping.md) · clause `8.2.2.12` · features `originalPortDefinition =`

```kebnf
ConjugatedPortTyping : ConjugatedPortTyping =
    '~' originalPortDefinition = ~[QualifiedName]

// (See Note 2)

// Notes:
//   1. Even though it is not explicitly represented in the text, a PortDefinition is always parsed as containing a nested ConjugatedPortDefinition with a PortDefinition Relationship pointing back to the containing PortDefinition. The abstract syntax for ConjugatedPortDefinition sets its effectiveName to the name of its originalPortDefinition with the symbol ~ prepended to it (see 8.3.12.2). (See also 8.4.8.1.)
//   2. The notation ~[QualifiedName] indicates that a QualifiedName shall be parsed from the input text, but that it shall be resolved as if it was the qualified name constructed as follows:
//     • Extract the last segment name of the given QualifiedName and prepend the symbol ~ to it.
//     • Append the name so constructed to the end of the entire original QualifiedName.
//   For example, if the ConjugatedPortTyping is ~A::B::C, then the given QualifiedName is A::B::C, and ~[QualifiedName] is resolved as A::B::C::'~C'. Alternatively, a conforming tool may first resolve the given QualifiedName as usual to a PortDefinition and then use the conjugatedPortDefinition of this PortDefinition as the resolution of ~[QualifiedName].
```

## 8.2.2.13.1 Connection Definition and Usage

### ConnectionDefinition

clause `8.2.2.13.1`

```kebnf
ConnectionDefinition =
    OccurrenceDefinitionPrefix 'connection' 'def' Definition
```

### ConnectionUsage

clause `8.2.2.13.1`

```kebnf
ConnectionUsage =
    OccurrenceUsagePrefix
    ( 'connection' UsageDeclaration ValuePart?
      ( 'connect' ConnectorPart )?
    | 'connect' ConnectorPart )
    UsageBody
```

### ConnectorPart

produces [ConnectionUsage](../metamodel/elements/ConnectionUsage.md) · clause `8.2.2.13.1`

```kebnf
ConnectorPart : ConnectionUsage =
    BinaryConnectorPart | NaryConnectorPart
```

### BinaryConnectorPart

produces [ConnectionUsage](../metamodel/elements/ConnectionUsage.md) · clause `8.2.2.13.1` · features `ownedRelationship +=`

```kebnf
BinaryConnectorPart : ConnectionUsage =
    ownedRelationship += ConnectorEndMember 'to'
    ownedRelationship += ConnectorEndMember
```

### NaryConnectorPart

produces [ConnectionUsage](../metamodel/elements/ConnectionUsage.md) · clause `8.2.2.13.1` · features `ownedRelationship +=`

```kebnf
NaryConnectorPart : ConnectionUsage =
    '(' ownedRelationship += ConnectorEndMember ','
        ownedRelationship += ConnectorEndMember
        ( ',' ownedRelationship += ConnectorEndMember )* ')'
```

### ConnectorEndMember

produces [EndFeatureMembership](../metamodel/elements/EndFeatureMembership.md) · clause `8.2.2.13.1` · features `ownedRelatedElement +=`

```kebnf
ConnectorEndMember : EndFeatureMembership =
    ownedRelatedElement += ConnectorEnd
```

### ConnectorEnd

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.13.1` · features `declaredName =`, `ownedRelationship +=`

```kebnf
ConnectorEnd : ReferenceUsage =
	( ownedRelationship += OwnedCrossMultiplicityMember )?
	( declaredName = NAME REFERENCES )?
	ownedRelationship += OwnedReferenceSubsetting
```

### OwnedCrossMultiplicityMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.13.1` · features `ownedRelatedElement +=`

```kebnf
OwnedCrossMultiplicityMember : OwningMembership =
	ownedRelatedElement += OwnedCrossMultiplicity
```

### OwnedCrossMultiplicity

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.13.1` · features `ownedRelationship +=`

```kebnf
OwnedCrossMultiplicity : Feature =
	ownedRelationship += OwnedMultiplicity
```

## 8.2.2.13.2 Binding Connectors

### BindingConnectorAsUsage

clause `8.2.2.13.2` · features `ownedRelationship +=`

```kebnf
BindingConnectorAsUsage =
    UsagePrefix ( 'binding' UsageDeclaration )?
    'bind' ownedRelationship += ConnectorEndMember
    '=' ownedRelationship += ConnectorEndMember
    UsageBody
```

## 8.2.2.13.3 Successions

### SuccessionAsUsage

clause `8.2.2.13.3` · features `ownedRelationship +=`

```kebnf
SuccessionAsUsage =
    UsagePrefix ( 'succession' UsageDeclaration )?
    'first' s.ownedRelationship += ConnectorEndMember
    'then' s.ownedRelationship += ConnectorEndMember
    UsageBody
```

## 8.2.2.14.1 Interface Definitions

### InterfaceDefinition

clause `8.2.2.14.1`

```kebnf
InterfaceDefinition =
    OccurrenceDefinitionPrefix 'interface' 'def'
    DefinitionDeclaration InterfaceBody
```

### InterfaceBody

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.14.1`

```kebnf
InterfaceBody : Type =
    ';' | '{' InterfaceBodyItem* '}'
```

### InterfaceBodyItem

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.14.1` · features `ownedRelationship +=`

```kebnf
InterfaceBodyItem : Type =
      ownedRelationship += DefinitionMember
    | ownedRelationship += VariantUsageMember
    | ownedRelationship += InterfaceNonOccurrenceUsageMember
    | ( ownedRelationship += SourceSuccessionMember )?
      ownedRelationship += InterfaceOccurrenceUsageMember
    | ownedRelationship += AliasMember
    | ownedRelationship += Import
```

### InterfaceNonOccurrenceUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.14.1` · features `ownedRelatedElement +=`

```kebnf
InterfaceNonOccurrenceUsageMember : FeatureMembership =
    MemberPrefix ownedRelatedElement += InterfaceNonOccurrenceUsageElement
```

### InterfaceNonOccurrenceUsageElement

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.14.1`

```kebnf
InterfaceNonOccurrenceUsageElement : Usage =
      ReferenceUsage
    | AttributeUsage
    | EnumerationUsage
    | BindingConnectorAsUsage
    | SuccessionAsUsage
```

### InterfaceOccurrenceUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.14.1` · features `ownedRelatedElement +=`

```kebnf
InterfaceOccurrenceUsageMember : FeatureMembership =
    MemberPrefix ownedRelatedElement += InterfaceOccurrenceUsageElement
```

### InterfaceOccurrenceUsageElement

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.14.1`

```kebnf
InterfaceOccurrenceUsageElement : Usage =
    DefaultInterfaceEnd | StructureUsageElement | BehaviorUsageElement
```

### DefaultInterfaceEnd

produces [PortUsage](../metamodel/elements/PortUsage.md) · clause `8.2.2.14.1` · features `isEnd ?=`

```kebnf
DefaultInterfaceEnd : PortUsage =
    isEnd ?= 'end' Usage
```

## 8.2.2.14.2 Interface Usages

### InterfaceUsage

clause `8.2.2.14.2`

```kebnf
InterfaceUsage =
    OccurrenceUsagePrefix 'interface'
    InterfaceUsageDeclaration InterfaceBody
```

### InterfaceUsageDeclaration

produces [InterfaceUsage](../metamodel/elements/InterfaceUsage.md) · clause `8.2.2.14.2`

```kebnf
InterfaceUsageDeclaration : InterfaceUsage =
      UsageDeclaration ValuePart?
      ( 'connect' InterfacePart )?
    | InterfacePart
```

### InterfacePart

produces [InterfaceUsage](../metamodel/elements/InterfaceUsage.md) · clause `8.2.2.14.2`

```kebnf
InterfacePart : InterfaceUsage =
    BinaryInterfacePart | NaryInterfacePart
```

### BinaryInterfacePart

produces [InterfaceUsage](../metamodel/elements/InterfaceUsage.md) · clause `8.2.2.14.2` · features `ownedRelationship +=`

```kebnf
BinaryInterfacePart : InterfaceUsage =
    ownedRelationship += InterfaceEndMember 'to'
    ownedRelationship += InterfaceEndMember
```

### NaryInterfacePart

produces [InterfaceUsage](../metamodel/elements/InterfaceUsage.md) · clause `8.2.2.14.2` · features `ownedRelationship +=`

```kebnf
NaryInterfacePart : InterfaceUsage =
    '(' ownedRelationship += InterfaceEndMember ','
        ownedRelationship += InterfaceEndMember
        ( ',' ownedRelationship += InterfaceEndMember )* ')'
```

### InterfaceEndMember

produces [EndFeatureMembership](../metamodel/elements/EndFeatureMembership.md) · clause `8.2.2.14.2` · features `ownedRelatedElement +=`

```kebnf
InterfaceEndMember : EndFeatureMembership =
    ownedRelatedElement += InterfaceEnd
```

### InterfaceEnd

produces [PortUsage](../metamodel/elements/PortUsage.md) · clause `8.2.2.14.2` · features `declaredName =`, `ownedRelationship +=`

```kebnf
InterfaceEnd : PortUsage =
    ( ownedRelationship += OwnedCrossMultiplicityMember )?
    ( declaredName = NAME REFERENCES )?
    ownedRelationship += OwnedReferenceSubsetting
```

## 8.2.2.15 Allocations Textual Notation

### AllocationDefinition

clause `8.2.2.15`

```kebnf
AllocationDefinition =
    OccurrenceDefinitionPrefix 'allocation' 'def' Definition
```

### AllocationUsage

clause `8.2.2.15`

```kebnf
AllocationUsage =
    OccurrenceUsagePrefix
    AllocationUsageDeclaration UsageBody
```

### AllocationUsageDeclaration

produces [AllocationUsage](../metamodel/elements/AllocationUsage.md) · clause `8.2.2.15`

```kebnf
AllocationUsageDeclaration : AllocationUsage =
      'allocation' UsageDeclaration
      ( 'allocate' ConnectorPart )?
    | 'allocate' ConnectorPart
```

## 8.2.2.16 Flows Textual Notation

### FlowDefinition

clause `8.2.2.16`

```kebnf
FlowDefinition =
    OccurrenceDefinitionPrefix 'flow' 'def' Definition
```

### Message

produces [FlowUsage](../metamodel/elements/FlowUsage.md) · clause `8.2.2.16` · features `isAbstract =`

```kebnf
Message : FlowUsage =
    OccurrenceUsagePrefix 'message'
    MessageDeclaration DefinitionBody
    { isAbstract = true }
```

### MessageDeclaration

produces [FlowUsage](../metamodel/elements/FlowUsage.md) · clause `8.2.2.16` · features `ownedRelationship +=`

```kebnf
MessageDeclaration : FlowUsage =
      UsageDeclaration ValuePart?
      ( 'of' ownedRelationship += FlowPayloadFeatureMember )?
      ( 'from' ownedRelationship += MessageEventMember
        'to' ownedRelationship += MessageEventMember
      )?
    | ownedRelationship += MessageEventMember 'to'
      ownedRelationship += MessageEventMember
```

### MessageEventMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.16` · features `ownedRelatedElement +=`

```kebnf
MessageEventMember : ParameterMembership =
    ownedRelatedElement += MessageEvent
```

### MessageEvent

produces [EventOccurrenceUsage](../metamodel/elements/EventOccurrenceUsage.md) · clause `8.2.2.16` · features `ownedRelationship +=`

```kebnf
MessageEvent : EventOccurrenceUsage =
    ownedRelationship += OwnedReferenceSubsetting
```

### FlowUsage

clause `8.2.2.16`

```kebnf
FlowUsage =
    OccurrenceUsagePrefix 'flow'
    FlowDeclaration DefinitionBody
```

### SuccessionFlowUsage

clause `8.2.2.16`

```kebnf
SuccessionFlowUsage =
    OccurrenceUsagePrefix 'succession' 'flow'
    FlowDeclaration DefinitionBody
```

### FlowDeclaration

produces [FlowUsage](../metamodel/elements/FlowUsage.md) · clause `8.2.2.16` · features `ownedRelationship +=`

```kebnf
FlowDeclaration : FlowUsage =
      UsageDeclaration ValuePart?
      ( 'of'  ownedRelationship += FlowPayloadFeatureMember )?
      ( 'from' ownedRelationship += FlowEndMember
        'to'   ownedRelationship += FlowEndMember )?
    | ownedRelationship += FlowEndMember 'to'
      ownedRelationship += FlowEndMember
```

### FlowPayloadFeatureMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.16` · features `ownedRelatedElement +=`

```kebnf
FlowPayloadFeatureMember : FeatureMembership =
    ownedRelatedElement += FlowPayloadFeature
```

### FlowPayloadFeature

produces [PayloadFeature](../metamodel/elements/PayloadFeature.md) · clause `8.2.2.16`

```kebnf
FlowPayloadFeature : PayloadFeature =
    PayloadFeature
```

### PayloadFeature

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.16` · features `ownedRelationship +=`

```kebnf
PayloadFeature : Feature =
      Identification? PayloadFeatureSpecializationPart
      ValuePart?
    | ownedRelationship += OwnedFeatureTyping
      ( ownedRelationship += OwnedMultiplicity )?
    | ownedRelationship += OwnedMultiplicity
      ownedRelationship += OwnedFeatureTyping
```

### PayloadFeatureSpecializationPart

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.16`

```kebnf
PayloadFeatureSpecializationPart : Feature =
      ( FeatureSpecialization )+ MultiplicityPart?
      FeatureSpecialization*
    | MultiplicityPart FeatureSpecialization+
```

### FlowEndMember

produces [EndFeatureMembership](../metamodel/elements/EndFeatureMembership.md) · clause `8.2.2.16` · features `ownedRelatedElement +=`

```kebnf
FlowEndMember : EndFeatureMembership =
    ownedRelatedElement += FlowEnd
```

### FlowEnd

clause `8.2.2.16` · features `ownedRelationship +=`

```kebnf
FlowEnd =
    ( ownedRelationship += FlowEndSubsetting )?
    ownedRelationship += FlowFeatureMember
```

### FlowEndSubsetting

produces [ReferenceSubsetting](../metamodel/elements/ReferenceSubsetting.md) · clause `8.2.2.16` · features `ownedRelatedElement +=`, `referencedFeature =`

```kebnf
FlowEndSubsetting : ReferenceSubsetting =
      referencedFeature = [QualifiedName]
    | referencedFeature = FeatureChainPrefix
      { ownedRelatedElement += referencedFeature }
```

### FeatureChainPrefix

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.16` · features `ownedRelationship +=`

```kebnf
FeatureChainPrefix : Feature =
    ( ownedRelationship += OwnedFeatureChaining '.' )+
    ownedRelationship += OwnedFeatureChaining '.'
```

### FlowFeatureMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.16` · features `ownedRelatedElement +=`

```kebnf
FlowFeatureMember : FeatureMembership =
    ownedRelatedElement += FlowFeature
```

### FlowFeature

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.16` · features `ownedRelationship +=`

```kebnf
FlowFeature : ReferenceUsage =
    ownedRelationship += FlowFeatureRedefinition

// (See Note 1)
```

### FlowFeatureRedefinition

produces [Redefinition](../metamodel/elements/Redefinition.md) · clause `8.2.2.16` · features `redefinedFeature =`

```kebnf
FlowFeatureRedefinition : Redefinition =
    redefinedFeature = [QualifiedName]

// Notes:
//   1. To ensure that a FlowFeature passes the validateRedefinitionDirectionConformance constraint (see [KerML, 8.3.3.3.8]), its direction must be set to the direction of its redefinedFeature, relative to its owning FlowEnd, that is, the result of the following OCL expression: owningType.directionOf(ownedRedefinition-&gt;at(1).redefinedFeature)
```

## 8.2.2.17.1 Action Definitions

### ActionDefinition

clause `8.2.2.17.1`

```kebnf
ActionDefinition =
    OccurrenceDefinitionPrefix 'action' 'def'
    DefinitionDeclaration ActionBody
```

### ActionBody

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.17.1`

```kebnf
ActionBody : Type =
    ';' | '{' ActionBodyItem* '}'
```

### ActionBodyItem

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.17.1` · features `ownedRelationship +=`

```kebnf
ActionBodyItem : Type =
      NonBehaviorBodyItem
    | ownedRelationship += InitialNodeMember
      ( ownedRelationship += ActionTargetSuccessionMember )*
    | ( ownedRelationship += SourceSuccessionMember )?
      ownedRelationship += ActionBehaviorMember
      ( ownedRelationship += ActionTargetSuccessionMember )*
    | ownedRelationship += GuardedSuccessionMember
```

### NonBehaviorBodyItem

clause `8.2.2.17.1` · features `ownedRelationship +=`

```kebnf
NonBehaviorBodyItem =
      ownedRelationship += Import
    | ownedRelationship += AliasMember
    | ownedRelationship += DefinitionMember
    | ownedRelationship += VariantUsageMember
    | ownedRelationship += NonOccurrenceUsageMember
    | ( ownedRelationship += SourceSuccessionMember )?
      ownedRelationship += StructureUsageMember
```

### ActionBehaviorMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.17.1`

```kebnf
ActionBehaviorMember : FeatureMembership =
    BehaviorUsageMember | ActionNodeMember
```

### InitialNodeMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.17.1` · features `memberFeature =`

```kebnf
InitialNodeMember : FeatureMembership =
    MemberPrefix 'first' memberFeature = [QualifiedName]
    RelationshipBody
```

### ActionNodeMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.17.1` · features `ownedRelatedElement +=`

```kebnf
ActionNodeMember : FeatureMembership =
    MemberPrefix ownedRelatedElement += ActionNode
```

### ActionTargetSuccessionMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.17.1` · features `ownedRelatedElement +=`

```kebnf
ActionTargetSuccessionMember : FeatureMembership =
    MemberPrefix ownedRelatedElement += ActionTargetSuccession
```

### GuardedSuccessionMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.17.1` · features `ownedRelatedElement +=`

```kebnf
GuardedSuccessionMember : FeatureMembership =
    MemberPrefix ownedRelatedElement += GuardedSuccession
```

## 8.2.2.17.2 Action Usages

### ActionUsage

clause `8.2.2.17.2`

```kebnf
ActionUsage =
    OccurrenceUsagePrefix 'action'
    ActionUsageDeclaration ActionBody
```

### ActionUsageDeclaration

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.17.2`

```kebnf
ActionUsageDeclaration : ActionUsage =
    UsageDeclaration ValuePart?
```

### PerformActionUsage

clause `8.2.2.17.2`

```kebnf
PerformActionUsage =
    OccurrenceUsagePrefix 'perform'
    PerformActionUsageDeclaration ActionBody
```

### PerformActionUsageDeclaration

produces [PerformActionUsage](../metamodel/elements/PerformActionUsage.md) · clause `8.2.2.17.2` · features `ownedRelationship +=`

```kebnf
PerformActionUsageDeclaration : PerformActionUsage =
    ( ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart?
    | 'action' UsageDeclaration )
    ValuePart?
```

### ActionNode

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.17.2`

```kebnf
ActionNode : ActionUsage =
      ControlNode
    | SendNode | AcceptNode
    | AssignmentNode
    | TerminateNode
    | IfNode | WhileLoopNode | ForLoopNode
```

### ActionNodeUsageDeclaration

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.17.2`

```kebnf
ActionNodeUsageDeclaration : ActionUsage =
    'action' UsageDeclaration?
```

### ActionNodePrefix

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.17.2`

```kebnf
ActionNodePrefix : ActionUsage =
    OccurrenceUsagePrefix ActionNodeUsageDeclaration?
```

## 8.2.2.17.3 Control Nodes

### ControlNode

clause `8.2.2.17.3`

```kebnf
ControlNode =
    MergeNode | DecisionNode | JoinNode| ForkNode
```

### ControlNodePrefix

produces [OccurrenceUsage](../metamodel/elements/OccurrenceUsage.md) · clause `8.2.2.17.3` · features `isIndividual ?=`, `isPortion =`, `portionKind =`

```kebnf
ControlNodePrefix : OccurrenceUsage =
    RefPrefix
    ( isIndividual ?= 'individual' )?
    ( portionKind = PortionKind
      { isPortion = true }
    )?
    UsageExtensionKeyword*
```

### MergeNode

clause `8.2.2.17.3` · features `isComposite ?=`

```kebnf
MergeNode =
    ControlNodePrefix
    isComposite ?= 'merge' UsageDeclaration
    ActionBody
```

### DecisionNode

clause `8.2.2.17.3` · features `isComposite ?=`

```kebnf
DecisionNode =
    ControlNodePrefix
    isComposite ?= 'decide' UsageDeclaration
    ActionBody
```

### JoinNode

clause `8.2.2.17.3` · features `isComposite ?=`

```kebnf
JoinNode =
    ControlNodePrefix
    isComposite ?= 'join' UsageDeclaration
    ActionBody
```

### ForkNode

clause `8.2.2.17.3` · features `isComposite ?=`

```kebnf
ForkNode =
    ControlNodePrefix
    isComposite ?= 'fork' UsageDeclaration
    ActionBody
```

## 8.2.2.17.4 Send and Accept Action Usages

### AcceptNode

produces [AcceptActionUsage](../metamodel/elements/AcceptActionUsage.md) · clause `8.2.2.17.4`

```kebnf
AcceptNode : AcceptActionUsage =
    OccurrenceUsagePrefix
    AcceptNodeDeclaration ActionBody
```

### AcceptNodeDeclaration

produces [AcceptActionUsage](../metamodel/elements/AcceptActionUsage.md) · clause `8.2.2.17.4`

```kebnf
AcceptNodeDeclaration : AcceptActionUsage =
    ActionNodeUsageDeclaration?
    'accept' AcceptParameterPart
```

### AcceptParameterPart

produces [AcceptActionUsage](../metamodel/elements/AcceptActionUsage.md) · clause `8.2.2.17.4` · features `ownedRelationship +=`

```kebnf
AcceptParameterPart : AcceptActionUsage =
    ownedRelationship += PayloadParameterMember
    ( 'via' ownedRelationship += NodeParameterMember )?
```

### PayloadParameterMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.4` · features `ownedRelatedElement +=`

```kebnf
PayloadParameterMember : ParameterMembership =
    ownedRelatedElement += PayloadParameter
```

### PayloadParameter

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.17.4`

```kebnf
PayloadParameter : ReferenceUsage =
      PayloadFeature
    | Identification PayloadFeatureSpecializationPart?
      TriggerValuePart
```

### TriggerValuePart

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.17.4` · features `ownedRelationship +=`

```kebnf
TriggerValuePart : Feature =
    ownedRelationship += TriggerFeatureValue
```

### TriggerFeatureValue

produces [FeatureValue](../metamodel/elements/FeatureValue.md) · clause `8.2.2.17.4` · features `ownedRelatedElement +=`

```kebnf
TriggerFeatureValue : FeatureValue =
    ownedRelatedElement += TriggerExpression
```

### TriggerExpression

produces [TriggerInvocationExpression](../metamodel/elements/TriggerInvocationExpression.md) · clause `8.2.2.17.4` · features `kind =`, `ownedRelationship +=`

```kebnf
TriggerExpression : TriggerInvocationExpression =
      kind = ( 'at' | 'after' )
      ownedRelationship += ArgumentMember
    | kind = 'when'
      ownedRelationship += ArgumentExpressionMember
```

### ArgumentMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.4` · features `ownedMemberParameter =`

```kebnf
ArgumentMember : ParameterMembership =
    ownedMemberParameter = Argument
```

### Argument

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.17.4` · features `ownedRelationship +=`

```kebnf
Argument : Feature =
    ownedRelationship += ArgumentValue
```

### ArgumentValue

produces [FeatureValue](../metamodel/elements/FeatureValue.md) · clause `8.2.2.17.4` · features `value =`

```kebnf
ArgumentValue : FeatureValue =
    value = OwnedExpression
```

### ArgumentExpressionMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.4` · features `ownedRelatedElement +=`

```kebnf
ArgumentExpressionMember : ParameterMembership =
    ownedRelatedElement += ArgumentExpression
```

### ArgumentExpression

produces [Feature](../metamodel/elements/Feature.md) · clause `8.2.2.17.4` · features `ownedRelationship +=`

```kebnf
ArgumentExpression : Feature =
    ownedRelationship += ArgumentExpressionValue
```

### ArgumentExpressionValue

produces [FeatureValue](../metamodel/elements/FeatureValue.md) · clause `8.2.2.17.4` · features `ownedRelatedElement +=`

```kebnf
ArgumentExpressionValue : FeatureValue =
    ownedRelatedElement += OwnedExpressionReference
```

### SendNode

produces [SendActionUsage](../metamodel/elements/SendActionUsage.md) · clause `8.2.2.17.4` · features `ownedRelationship +=`

```kebnf
SendNode : SendActionUsage =
    OccurrenceUsagePrefix ActionUsageDeclaration? 'send'
    ( ownedRelationship += NodeParameterMember SenderReceiverPart?
    | ownedRelationship += EmptyParameterMember SenderReceiverPart )?
    ActionBody
```

### SendNodeDeclaration

produces [SendActionUsage](../metamodel/elements/SendActionUsage.md) · clause `8.2.2.17.4` · features `ownedRelationship +=`

```kebnf
SendNodeDeclaration : SendActionUsage =
    ActionNodeUsageDeclaration?  'send'
    ownedRelationship += NodeParameterMember SenderReceiverPart?
```

### SenderReceiverPart

produces [SendActionUsage](../metamodel/elements/SendActionUsage.md) · clause `8.2.2.17.4` · features `ownedRelationship +=`

```kebnf
SenderReceiverPart : SendActionUsage =
      'via' ownedRelationship += NodeParameterMember
      ( 'to' ownedRelationship += NodeParameterMember )?
    | ownedRelationship += EmptyParameterMember
      'to' ownedRelationship += NodeParameterMember
```

### NodeParameterMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.4` · features `ownedRelatedElement +=`

```kebnf
NodeParameterMember : ParameterMembership =
    ownedRelatedElement += NodeParameter
```

### NodeParameter

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.17.4` · features `ownedRelationship +=`

```kebnf
NodeParameter : ReferenceUsage =
    ownedRelationship += FeatureBinding
```

### FeatureBinding

produces [FeatureValue](../metamodel/elements/FeatureValue.md) · clause `8.2.2.17.4` · features `ownedRelatedElement +=`

```kebnf
FeatureBinding : FeatureValue =
    ownedRelatedElement += OwnedExpression
```

### EmptyParameterMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.4` · features `ownedRelatedElement +=`

```kebnf
EmptyParameterMember : ParameterMembership =
    ownedRelatedElement += EmptyUsage
```

### EmptyUsage

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.17.4`

```kebnf
EmptyUsage : ReferenceUsage =
    {}

// Notes:
//   1. The productions for ArgumentMember, Argument, ArgumentValue, ArgumentExpressionMember, ArgumentExpression and ArgumentExpressionValue are the same as given in [KerML, 8.2.5.8.1].
```

## 8.2.2.17.5 Assignment Action Usages

### AssignmentNode

produces [AssignmentActionUsage](../metamodel/elements/AssignmentActionUsage.md) · clause `8.2.2.17.5`

```kebnf
AssignmentNode : AssignmentActionUsage =
    OccurrenceUsagePrefix
    AssignmentNodeDeclaration ActionBody
```

### AssignmentNodeDeclaration

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.17.5` · features `ownedRelationship +=`

```kebnf
AssignmentNodeDeclaration: ActionUsage =
    ( ActionNodeUsageDeclaration )? 'assign'
    ownedRelationship += AssignmentTargetMember
    ownedRelationship += FeatureChainMember ':='
    ownedRelationship += NodeParameterMember
```

### AssignmentTargetMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.5` · features `ownedRelatedElement +=`

```kebnf
AssignmentTargetMember : ParameterMembership =
    ownedRelatedElement += AssignmentTargetParameter
```

### AssignmentTargetParameter

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.17.5` · features `ownedRelationship +=`

```kebnf
AssignmentTargetParameter : ReferenceUsage =
    ( ownedRelationship += AssignmentTargetBinding '.' )?
```

### AssignmentTargetBinding

produces [FeatureValue](../metamodel/elements/FeatureValue.md) · clause `8.2.2.17.5` · features `ownedRelatedElement +=`

```kebnf
AssignmentTargetBinding : FeatureValue =
    ownedRelatedElement += NonFeatureChainPrimaryExpression
```

### FeatureChainMember

produces [Membership](../metamodel/elements/Membership.md) · clause `8.2.2.17.5` · features `memberElement =`

```kebnf
FeatureChainMember : Membership =
      memberElement = [QualifiedName]
    | OwnedFeatureChainMember
```

### OwnedFeatureChainMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.17.5` · features `ownedRelatedElement +=`

```kebnf
OwnedFeatureChainMember : OwningMembership =
    ownedRelatedElement += OwnedFeatureChain
```

## 8.2.2.17.6 Terminate Action Usages

### TerminateNode

produces [TerminateActionUsage](../metamodel/elements/TerminateActionUsage.md) · clause `8.2.2.17.6` · features `ownedRelationship +=`

```kebnf
TerminateNode : TerminateActionUsage =
    OccurrenceUsagePrefix ActionNodeUsageDeclaration?
    'terminate' ( ownedRelationship += NodeParameterMember )?
    ActionBody
```

## 8.2.2.17.7 Structured Control Action Usages

### IfNode

produces [IfActionUsage](../metamodel/elements/IfActionUsage.md) · clause `8.2.2.17.7` · features `ownedRelationship +=`

```kebnf
IfNode : IfActionUsage =
    ActionNodePrefix
    'if' ownedRelationship += ExpressionParameterMember
    ownedRelationship += ActionBodyParameterMember
    ( 'else' ownedRelationship +=
      ( ActionBodyParameterMember | IfNodeParameterMember ) )?
```

### ExpressionParameterMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.7` · features `ownedRelatedElement +=`

```kebnf
ExpressionParameterMember : ParameterMembership =
    ownedRelatedElement += OwnedExpression
```

### ActionBodyParameterMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.7` · features `ownedRelatedElement +=`

```kebnf
ActionBodyParameterMember : ParameterMembership =
    ownedRelatedElement += ActionBodyParameter
```

### ActionBodyParameter

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.17.7`

```kebnf
ActionBodyParameter : ActionUsage =
    ( 'action' UsageDeclaration? )?
    '{' ActionBodyItem* '}'
```

### IfNodeParameterMember

produces [ParameterMembership](../metamodel/elements/ParameterMembership.md) · clause `8.2.2.17.7` · features `ownedRelatedElement +=`

```kebnf
IfNodeParameterMember : ParameterMembership =
    ownedRelatedElement += IfNode
```

### WhileLoopNode

produces [WhileLoopActionUsage](../metamodel/elements/WhileLoopActionUsage.md) · clause `8.2.2.17.7` · features `ownedRelationship +=`

```kebnf
WhileLoopNode : WhileLoopActionUsage =
    ActionNodePrefix
    ( 'while' ownedRelationship += ExpressionParameterMember
    | 'loop' ownedRelationship += EmptyParameterMember
    )
    ownedRelationship += ActionBodyParameterMember
    ( 'until' ownedRelationship += ExpressionParameterMember ';' )?
```

### ForLoopNode

produces [ForLoopActionUsage](../metamodel/elements/ForLoopActionUsage.md) · clause `8.2.2.17.7` · features `ownedRelationship +=`

```kebnf
ForLoopNode : ForLoopActionUsage =
    ActionNodePrefix
    'for' ownedRelationship += ForVariableDeclarationMember
    'in' ownedRelationship += NodeParameterMember
    ownedRelationship += ActionBodyParameterMember
```

### ForVariableDeclarationMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.17.7` · features `ownedRelatedElement +=`

```kebnf
ForVariableDeclarationMember : FeatureMembership =
    ownedRelatedElement += UsageDeclaration
```

### ForVariableDeclaration

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.17.7`

```kebnf
ForVariableDeclaration : ReferenceUsage =
    UsageDeclaration
```

## 8.2.2.17.8 Action Successions

### ActionTargetSuccession

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.17.8`

```kebnf
ActionTargetSuccession : Usage =
    ( TargetSuccession | GuardedTargetSuccession | DefaultTargetSuccession )
    UsageBody
```

### TargetSuccession

produces [SuccessionAsUsage](../metamodel/elements/SuccessionAsUsage.md) · clause `8.2.2.17.8` · features `ownedRelationship +=`

```kebnf
TargetSuccession : SuccessionAsUsage =
    ownedRelationship += SourceEndMember
    'then' ownedRelationship += ConnectorEndMember
```

### GuardedTargetSuccession

produces [TransitionUsage](../metamodel/elements/TransitionUsage.md) · clause `8.2.2.17.8` · features `ownedRelationship +=`

```kebnf
GuardedTargetSuccession : TransitionUsage =
    ownedRelationship += GuardExpressionMember
    'then' ownedRelationship += TransitionSuccessionMember
```

### DefaultTargetSuccession

produces [TransitionUsage](../metamodel/elements/TransitionUsage.md) · clause `8.2.2.17.8` · features `ownedRelationship +=`

```kebnf
DefaultTargetSuccession : TransitionUsage =
    'else' ownedRelationship += TransitionSuccessionMember
```

### GuardedSuccession

produces [TransitionUsage](../metamodel/elements/TransitionUsage.md) · clause `8.2.2.17.8` · features `ownedRelationship +=`

```kebnf
GuardedSuccession : TransitionUsage =
    ( 'succession' UsageDeclaration )?
    'first' ownedRelationship += FeatureChainMember
    ownedRelationship += GuardExpressionMember
    'then' ownedRelationship += TransitionSuccessionMember
    UsageBody
```

## 8.2.2.18.1 State Definitions

### StateDefinition

clause `8.2.2.18.1`

```kebnf
StateDefinition =
    OccurrenceDefinitionPrefix 'state' 'def'
    DefinitionDeclaration StateDefBody
```

### StateDefBody

produces [StateDefinition](../metamodel/elements/StateDefinition.md) · clause `8.2.2.18.1` · features `isParallel ?=`

```kebnf
StateDefBody : StateDefinition =
      ';'
    | ( isParallel ?= 'parallel' )?
      '{' StateBodyItem* '}'
```

### StateBodyItem

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.18.1` · features `ownedRelationship +=`

```kebnf
StateBodyItem : Type =
      NonBehaviorBodyItem
    | ( ownedRelationship += SourceSuccessionMember )?
      ownedRelationship += BehaviorUsageMember
      ( ownedRelationship += TargetTransitionUsageMember )*
    | ownedRelationship += TransitionUsageMember
    | ownedRelationship += EntryActionMember
      ( ownedRelationship += EntryTransitionMember )*
    | ownedRelationship += DoActionMember
    | ownedRelationship += ExitActionMember
```

### EntryActionMember

produces [StateSubactionMembership](../metamodel/elements/StateSubactionMembership.md) · clause `8.2.2.18.1` · features `kind =`, `ownedRelatedElement +=`

```kebnf
EntryActionMember : StateSubactionMembership =
    MemberPrefix kind = 'entry'
    ownedRelatedElement += StateActionUsage
```

### DoActionMember

produces [StateSubactionMembership](../metamodel/elements/StateSubactionMembership.md) · clause `8.2.2.18.1` · features `kind =`, `ownedRelatedElement +=`

```kebnf
DoActionMember : StateSubactionMembership =
    MemberPrefix kind = 'do'
    ownedRelatedElement += StateActionUsage
```

### ExitActionMember

produces [StateSubactionMembership](../metamodel/elements/StateSubactionMembership.md) · clause `8.2.2.18.1` · features `kind =`, `ownedRelatedElement +=`

```kebnf
ExitActionMember : StateSubactionMembership =
    MemberPrefix kind = 'exit'
    ownedRelatedElement += StateActionUsage
```

### EntryTransitionMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.18.1` · features `ownedRelatedElement +=`

```kebnf
EntryTransitionMember : FeatureMembership =
    MemberPrefix
    ( ownedRelatedElement += GuardedTargetSuccession
    | 'then' ownedRelatedElement += TargetSuccession
    ) ';'
```

### StateActionUsage

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.18.1`

```kebnf
StateActionUsage : ActionUsage =
    EmptyActionUsage ';'
    | StatePerformActionUsage
    | StateAcceptActionUsage
    | StateSendActionUsage
    | StateAssignmentActionUsage
```

### EmptyActionUsage

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.18.1`

```kebnf
EmptyActionUsage : ActionUsage =
    {}
```

### StatePerformActionUsage

produces [PerformActionUsage](../metamodel/elements/PerformActionUsage.md) · clause `8.2.2.18.1`

```kebnf
StatePerformActionUsage : PerformActionUsage =
    PerformActionUsageDeclaration ActionBody
```

### StateAcceptActionUsage

produces [AcceptActionUsage](../metamodel/elements/AcceptActionUsage.md) · clause `8.2.2.18.1`

```kebnf
StateAcceptActionUsage : AcceptActionUsage =
    AcceptNodeDeclaration ActionBody
```

### StateSendActionUsage

produces [SendActionUsage](../metamodel/elements/SendActionUsage.md) · clause `8.2.2.18.1`

```kebnf
StateSendActionUsage : SendActionUsage =
    SendNodeDeclaration ActionBody
```

### StateAssignmentActionUsage

produces [AssignmentActionUsage](../metamodel/elements/AssignmentActionUsage.md) · clause `8.2.2.18.1`

```kebnf
StateAssignmentActionUsage : AssignmentActionUsage =
    AssignmentNodeDeclaration ActionBody
```

### TransitionUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.18.1` · features `ownedRelatedElement +=`

```kebnf
TransitionUsageMember : FeatureMembership =
    MemberPrefix ownedRelatedElement += TransitionUsage
```

### TargetTransitionUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.18.1` · features `ownedRelatedElement +=`

```kebnf
TargetTransitionUsageMember : FeatureMembership =
    MemberPrefix ownedRelatedElement += TargetTransitionUsage
```

## 8.2.2.18.2 State Usages

### StateUsage

clause `8.2.2.18.2`

```kebnf
StateUsage =
    OccurrenceUsagePrefix 'state'
    ActionUsageDeclaration StateUsageBody
```

### StateUsageBody

produces [StateUsage](../metamodel/elements/StateUsage.md) · clause `8.2.2.18.2` · features `isParallel ?=`

```kebnf
StateUsageBody : StateUsage =
       ';'
     | ( isParallel ?= 'parallel' )?
       '{' StateBodyItem* '}'
```

### ExhibitStateUsage

clause `8.2.2.18.2` · features `ownedRelationship +=`

```kebnf
ExhibitStateUsage =
    OccurrenceUsagePrefix 'exhibit'
    ( ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart?
    | 'state' UsageDeclaration )
    ValuePart? StateUsageBody
```

## 8.2.2.18.3 Transition Usages

### TransitionUsage

clause `8.2.2.18.3` · features `ownedRelationship +=`

```kebnf
TransitionUsage =
    'transition' ( UsageDeclaration 'first' )?
    ownedRelationship += FeatureChainMember
    ownedRelationship += EmptyParameterMember
    ( ownedRelationship += EmptyParameterMember
      ownedRelationship += TriggerActionMember )?
    ( ownedRelationship += GuardExpressionMember )?
    ( ownedRelationship += EffectBehaviorMember )?
    'then' ownedRelationship += TransitionSuccessionMember
    ActionBody
```

### TargetTransitionUsage

produces [TransitionUsage](../metamodel/elements/TransitionUsage.md) · clause `8.2.2.18.3` · features `ownedRelationship +=`

```kebnf
TargetTransitionUsage : TransitionUsage =
    ownedRelationship += EmptyParameterMember
    ( 'transition'
      ( ownedRelationship += EmptyParameterMember
        ownedRelationship += TriggerActionMember )?
      ( ownedRelationship += GuardExpressionMember )?
      ( ownedRelationship += EffectBehaviorMember )?
    | ownedRelationship += EmptyParameterMember
      ownedRelationship += TriggerActionMember
      ( ownedRelationship += GuardExpressionMember )?
      ( ownedRelationship += EffectBehaviorMember )?
    | ownedRelationship += GuardExpressionMember
      ( ownedRelationship += EffectBehaviorMember )?
    )?
    'then' ownedRelationship += TransitionSuccessionMember
    ActionBody
```

### TriggerActionMember

produces [TransitionFeatureMembership](../metamodel/elements/TransitionFeatureMembership.md) · clause `8.2.2.18.3` · features `kind =`, `ownedRelatedElement +=`

```kebnf
TriggerActionMember : TransitionFeatureMembership =
    'accept' { kind = 'trigger' } ownedRelatedElement += TriggerAction
```

### TriggerAction

produces [AcceptActionUsage](../metamodel/elements/AcceptActionUsage.md) · clause `8.2.2.18.3`

```kebnf
TriggerAction : AcceptActionUsage =
    AcceptParameterPart
```

### GuardExpressionMember

produces [TransitionFeatureMembership](../metamodel/elements/TransitionFeatureMembership.md) · clause `8.2.2.18.3` · features `kind =`, `ownedRelatedElement +=`

```kebnf
GuardExpressionMember : TransitionFeatureMembership =
    'if' { kind = 'guard' } ownedRelatedElement += OwnedExpression
```

### EffectBehaviorMember

produces [TransitionFeatureMembership](../metamodel/elements/TransitionFeatureMembership.md) · clause `8.2.2.18.3` · features `kind =`, `ownedRelatedElement +=`

```kebnf
EffectBehaviorMember : TransitionFeatureMembership =
    'do' { kind = 'effect' } ownedRelatedElement += EffectBehaviorUsage
```

### EffectBehaviorUsage

produces [ActionUsage](../metamodel/elements/ActionUsage.md) · clause `8.2.2.18.3`

```kebnf
EffectBehaviorUsage : ActionUsage =
      EmptyActionUsage
    | TransitionPerformActionUsage
    | TransitionAcceptActionUsage
    | TransitionSendActionUsage
    | TransitionAssignmentActionUsage
```

### TransitionPerformActionUsage

produces [PerformActionUsage](../metamodel/elements/PerformActionUsage.md) · clause `8.2.2.18.3`

```kebnf
TransitionPerformActionUsage : PerformActionUsage =
    PerformActionUsageDeclaration ( '{' ActionBodyItem* '}' )?
```

### TransitionAcceptActionUsage

produces [AcceptActionUsage](../metamodel/elements/AcceptActionUsage.md) · clause `8.2.2.18.3`

```kebnf
TransitionAcceptActionUsage : AcceptActionUsage =
    AcceptNodeDeclaration ( '{' ActionBodyItem* '}' )?
```

### TransitionSendActionUsage

produces [SendActionUsage](../metamodel/elements/SendActionUsage.md) · clause `8.2.2.18.3`

```kebnf
TransitionSendActionUsage : SendActionUsage =
    SendNodeDeclaration ( '{' ActionBodyItem* '}' )?
```

### TransitionAssignmentActionUsage

produces [AssignmentActionUsage](../metamodel/elements/AssignmentActionUsage.md) · clause `8.2.2.18.3`

```kebnf
TransitionAssignmentActionUsage : AssignmentActionUsage =
    AssignmentNodeDeclaration ( '{' ActionBodyItem* '}' )?
```

### TransitionSuccessionMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.18.3` · features `ownedRelatedElement +=`

```kebnf
TransitionSuccessionMember : OwningMembership =
    ownedRelatedElement += TransitionSuccession
```

### TransitionSuccession

produces [Succession](../metamodel/elements/Succession.md) · clause `8.2.2.18.3` · features `ownedRelationship +=`

```kebnf
TransitionSuccession : Succession =
    ownedRelationship += EmptyEndMember
    ownedRelationship += ConnectorEndMember
```

### EmptyEndMember

produces [EndFeatureMembership](../metamodel/elements/EndFeatureMembership.md) · clause `8.2.2.18.3` · features `ownedRelatedElement +=`

```kebnf
EmptyEndMember : EndFeatureMembership =
    ownedRelatedElement += EmptyFeature
```

### EmptyFeature

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.18.3`

```kebnf
EmptyFeature : ReferenceUsage =
    {}
```

## 8.2.2.19 Calculations Textual Notation

### CalculationDefinition

clause `8.2.2.19`

```kebnf
CalculationDefinition =
    OccurrenceDefinitionPrefix 'calc' 'def'
    DefinitionDeclaration CalculationBody
```

### CalculationUsage

produces [CalculationUsage](../metamodel/elements/CalculationUsage.md) · clause `8.2.2.19`

```kebnf
CalculationUsage : CalculationUsage =
    OccurrenceUsagePrefix 'calc'
    ActionUsageDeclaration CalculationBody
```

### CalculationBody

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.19`

```kebnf
CalculationBody : Type =
      ';' | '{' CalculationBodyPart '}'
```

### CalculationBodyPart

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.19` · features `ownedRelationship +=`

```kebnf
CalculationBodyPart : Type =
      CalculationBodyItem*
      ( ownedRelationship += ResultExpressionMember )?
```

### CalculationBodyItem

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.19` · features `ownedRelationship +=`

```kebnf
CalculationBodyItem : Type =
      ActionBodyItem
    | ownedRelationship += ReturnParameterMember
```

### ReturnParameterMember

produces [ReturnParameterMembership](../metamodel/elements/ReturnParameterMembership.md) · clause `8.2.2.19` · features `ownedRelatedElement +=`

```kebnf
ReturnParameterMember : ReturnParameterMembership =
    MemberPrefix? 'return' ownedRelatedElement += UsageElement
```

### ResultExpressionMember

produces [ResultExpressionMembership](../metamodel/elements/ResultExpressionMembership.md) · clause `8.2.2.19` · features `ownedRelatedElement +=`

```kebnf
ResultExpressionMember : ResultExpressionMembership =
    MemberPrefix? ownedRelatedElement += OwnedExpression
```

## 8.2.2.20 Constraints Textual Notation

### ConstraintDefinition

clause `8.2.2.20`

```kebnf
ConstraintDefinition =
    OccurrenceDefinitionPrefix 'constraint' 'def'
    DefinitionDeclaration CalculationBody
```

### ConstraintUsage

clause `8.2.2.20`

```kebnf
ConstraintUsage =
    OccurrenceUsagePrefix 'constraint'
    ConstraintUsageDeclaration CalculationBody
```

### AssertConstraintUsage

clause `8.2.2.20` · features `isNegated ?=`, `ownedRelationship +=`

```kebnf
AssertConstraintUsage =
    OccurrenceUsagePrefix 'assert' ( isNegated ?= 'not' )?
    ( ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart?
    | 'constraint' ConstraintUsageDeclaration )
    CalculationBody
```

### ConstraintUsageDeclaration

produces [ConstraintUsage](../metamodel/elements/ConstraintUsage.md) · clause `8.2.2.20`

```kebnf
ConstraintUsageDeclaration : ConstraintUsage =
    UsageDeclaration ValuePart?
```

## 8.2.2.21.1 Requirement Definitions

### RequirementDefinition

clause `8.2.2.21.1`

```kebnf
RequirementDefinition =
    OccurrenceDefinitionPrefix 'requirement' 'def'
    DefinitionDeclaration RequirementBody
```

### RequirementBody

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.21.1`

```kebnf
RequirementBody : Type =
      ';' | '{' RequirementBodyItem* '}'
```

### RequirementBodyItem

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.21.1` · features `ownedRelationship +=`

```kebnf
RequirementBodyItem : Type =
      DefinitionBodyItem
    | ownedRelationship += SubjectMember
    | ownedRelationship += RequirementConstraintMember
    | ownedRelationship += FramedConcernMember
    | ownedRelationship += RequirementVerificationMember
    | ownedRelationship += ActorMember
    | ownedRelationship += StakeholderMember
```

### SubjectMember

produces [SubjectMembership](../metamodel/elements/SubjectMembership.md) · clause `8.2.2.21.1` · features `ownedRelatedElement +=`

```kebnf
SubjectMember : SubjectMembership =
    MemberPrefix ownedRelatedElement += SubjectUsage
```

### SubjectUsage

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.21.1`

```kebnf
SubjectUsage : ReferenceUsage =
    'subject' UsageExtensionKeyword* Usage
```

### RequirementConstraintMember

produces [RequirementConstraintMembership](../metamodel/elements/RequirementConstraintMembership.md) · clause `8.2.2.21.1` · features `ownedRelatedElement +=`

```kebnf
RequirementConstraintMember : RequirementConstraintMembership =
    MemberPrefix? RequirementKind
    ownedRelatedElement += RequirementConstraintUsage
```

### RequirementKind

produces [RequirementConstraintMembership](../metamodel/elements/RequirementConstraintMembership.md) · clause `8.2.2.21.1` · features `kind =`

```kebnf
RequirementKind : RequirementConstraintMembership =
      'assume' { kind = 'assumption' }
    | 'require' { kind = 'requirement' }
```

### RequirementConstraintUsage

produces [ConstraintUsage](../metamodel/elements/ConstraintUsage.md) · clause `8.2.2.21.1` · features `ownedRelationship +=`

```kebnf
RequirementConstraintUsage : ConstraintUsage =
      ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart? RequirementBody
    | ( UsageExtensionKeyword* 'constraint'
      | UsageExtensionKeyword+ )
      ConstraintUsageDeclaration CalculationBody
```

### FramedConcernMember

produces [FramedConcernMembership](../metamodel/elements/FramedConcernMembership.md) · clause `8.2.2.21.1` · features `ownedRelatedElement +=`

```kebnf
FramedConcernMember : FramedConcernMembership =
    MemberPrefix? 'frame'
    ownedRelatedElement += FramedConcernUsage
```

### FramedConcernUsage

produces [ConcernUsage](../metamodel/elements/ConcernUsage.md) · clause `8.2.2.21.1` · features `ownedRelationship +=`

```kebnf
FramedConcernUsage : ConcernUsage =
      ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart? CalculationBody
    | ( UsageExtensionKeyword* 'concern'
      | UsageExtensionKeyword+ )
      CalculationUsageDeclaration CalculationBody
```

### ActorMember

produces [ActorMembership](../metamodel/elements/ActorMembership.md) · clause `8.2.2.21.1` · features `ownedRelatedElement +=`

```kebnf
ActorMember : ActorMembership =
    MemberPrefix ownedRelatedElement += ActorUsage
```

### ActorUsage

produces [PartUsage](../metamodel/elements/PartUsage.md) · clause `8.2.2.21.1`

```kebnf
ActorUsage : PartUsage =
    'actor' UsageExtensionKeyword* Usage
```

### StakeholderMember

produces [StakeholderMembership](../metamodel/elements/StakeholderMembership.md) · clause `8.2.2.21.1` · features `ownedRelatedElement +=`

```kebnf
StakeholderMember : StakeholderMembership =
    MemberPrefix ownedRelatedElement += StakeholderUsage
```

### StakeholderUsage

produces [PartUsage](../metamodel/elements/PartUsage.md) · clause `8.2.2.21.1`

```kebnf
StakeholderUsage : PartUsage =
    'stakeholder' UsageExtensionKeyword* Usage
```

## 8.2.2.21.2 Requirement Usages

### RequirementUsage

clause `8.2.2.21.2`

```kebnf
RequirementUsage =
    OccurrenceUsagePrefix 'requirement'
    ConstraintUsageDeclaration RequirementBody
```

### SatisfyRequirementUsage

clause `8.2.2.21.2` · features `isNegated ?=`, `ownedRelationship +=`

```kebnf
SatisfyRequirementUsage =
    OccurrenceUsagePrefix 'assert' ( isNegated ?= 'not' ) 'satisfy'
    ( ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart?
    | 'requirement' UsageDeclaration )
    ValuePart?
    ( 'by' ownedRelationship += SatisfactionSubjectMember )?
    RequirementBody
```

### SatisfactionSubjectMember

produces [SubjectMembership](../metamodel/elements/SubjectMembership.md) · clause `8.2.2.21.2` · features `ownedRelatedElement +=`

```kebnf
SatisfactionSubjectMember : SubjectMembership =
    ownedRelatedElement += SatisfactionParameter
```

### SatisfactionParameter

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.21.2` · features `ownedRelationship +=`

```kebnf
SatisfactionParameter : ReferenceUsage =
    ownedRelationship += SatisfactionFeatureValue
```

### SatisfactionFeatureValue

produces [FeatureValue](../metamodel/elements/FeatureValue.md) · clause `8.2.2.21.2` · features `ownedRelatedElement +=`

```kebnf
SatisfactionFeatureValue : FeatureValue =
    ownedRelatedElement += SatisfactionReferenceExpression
```

### SatisfactionReferenceExpression

produces [FeatureReferenceExpression](../metamodel/elements/FeatureReferenceExpression.md) · clause `8.2.2.21.2` · features `ownedRelationship +=`

```kebnf
SatisfactionReferenceExpression : FeatureReferenceExpression =
    ownedRelationship += FeatureChainMember
```

## 8.2.2.21.3 Concerns

### ConcernDefinition

clause `8.2.2.21.3`

```kebnf
ConcernDefinition =
    OccurrenceDefinitionPrefix 'concern' 'def'
    DefinitionDeclaration RequirementBody
```

### ConcernUsage

clause `8.2.2.21.3`

```kebnf
ConcernUsage =
    OccurrenceUsagePrefix 'concern'
    ConstraintUsageDeclaration RequirementBody
```

## 8.2.2.22 Cases Textual Notation

### CaseDefinition

clause `8.2.2.22`

```kebnf
CaseDefinition =
    OccurrenceDefinitionPrefix 'case' 'def'
    DefinitionDeclaration CaseBody
```

### CaseUsage

clause `8.2.2.22`

```kebnf
CaseUsage =
    OccurrenceUsagePrefix 'case'
    ConstraintUsageDeclaration CaseBody
```

### CaseBody

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.22` · features `ownedRelationship +=`

```kebnf
CaseBody : Type =
      ';'
    | '{' CaseBodyItem*
          ( ownedRelationship += ResultExpressionMember )?
      '}'
```

### CaseBodyItem

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.22` · features `ownedRelationship +=`

```kebnf
CaseBodyItem : Type =
      ActionBodyItem
    | ownedRelationship += SubjectMember
    | ownedRelationship += ActorMember
    | ownedRelationship += ObjectiveMember
```

### ObjectiveMember

produces [ObjectiveMembership](../metamodel/elements/ObjectiveMembership.md) · clause `8.2.2.22` · features `ownedRelatedElement +=`

```kebnf
ObjectiveMember : ObjectiveMembership =
    MemberPrefix 'objective'
    ownedRelatedElement += ObjectiveRequirementUsage
```

### ObjectiveRequirementUsage

produces [RequirementUsage](../metamodel/elements/RequirementUsage.md) · clause `8.2.2.22`

```kebnf
ObjectiveRequirementUsage : RequirementUsage =
    UsageExtensionKeyword* ConstraintUsageDeclaration
    RequirementBody
```

## 8.2.2.23 Analysis Cases Textual Notation

### AnalysisCaseDefinition

clause `8.2.2.23`

```kebnf
AnalysisCaseDefinition =
    OccurrenceDefinitionPrefix 'analysis' 'def'
    DefinitionDeclaration CaseBody
```

### AnalysisCaseUsage

clause `8.2.2.23`

```kebnf
AnalysisCaseUsage =
    OccurrenceUsagePrefix 'analysis'
    ConstraintUsageDeclaration CaseBody
```

## 8.2.2.24 Verification Cases Textual Notation

### VerificationCaseDefinition

clause `8.2.2.24`

```kebnf
VerificationCaseDefinition =
    OccurrenceDefinitionPrefix 'verification' 'def'
    DefinitionDeclaration CaseBody
```

### VerificationCaseUsage

clause `8.2.2.24`

```kebnf
VerificationCaseUsage =
    OccurrenceUsagePrefix 'verification'
    ConstraintUsageDeclaration CaseBody
```

### RequirementVerificationMember

produces [RequirementVerificationMembership](../metamodel/elements/RequirementVerificationMembership.md) · clause `8.2.2.24` · features `kind =`, `ownedRelatedElement +=`

```kebnf
RequirementVerificationMember : RequirementVerificationMembership =
    MemberPrefix 'verify' { kind = 'requirement' }
    ownedRelatedElement += RequirementVerificationUsage
```

### RequirementVerificationUsage

produces [RequirementUsage](../metamodel/elements/RequirementUsage.md) · clause `8.2.2.24` · features `ownedRelationship +=`

```kebnf
RequirementVerificationUsage : RequirementUsage =
      ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecialization* RequirementBody
    | ( UsageExtensionKeyword* 'requirement'
      | UsageExtensionKeyword+ )
      ConstraintUsageDeclaration RequirementBody
```

## 8.2.2.25 Use Cases Textual Notation

### UseCaseDefinition

clause `8.2.2.25`

```kebnf
UseCaseDefinition =
    OccurrenceDefinitionPrefix 'use' 'case' 'def'
    DefinitionDeclaration CaseBody
```

### UseCaseUsage

clause `8.2.2.25`

```kebnf
UseCaseUsage =
    OccurrenceUsagePrefix 'use' 'case'
    ConstraintUsageDeclaration CaseBody
```

### IncludeUseCaseUsage

clause `8.2.2.25` · features `ownedRelationship +=`

```kebnf
IncludeUseCaseUsage =
    OccurrenceUsagePrefix 'include'
    ( ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart?
    | 'use' 'case' UsageDeclaration )
    ValuePart?
    CaseBody
```

## 8.2.2.26.1 View Definitions

### ViewDefinition

clause `8.2.2.26.1`

```kebnf
ViewDefinition =
    OccurrenceDefinitionPrefix 'view' 'def'
    DefinitionDeclaration ViewDefinitionBody
```

### ViewDefinitionBody

produces [ViewDefinition](../metamodel/elements/ViewDefinition.md) · clause `8.2.2.26.1`

```kebnf
ViewDefinitionBody : ViewDefinition =
    ';' | '{' ViewDefinitionBodyItem* '}'
```

### ViewDefinitionBodyItem

produces [ViewDefinition](../metamodel/elements/ViewDefinition.md) · clause `8.2.2.26.1` · features `ownedRelationship +=`

```kebnf
ViewDefinitionBodyItem : ViewDefinition =
      DefinitionBodyItem
    | ownedRelationship += ElementFilterMember
    | ownedRelationship += ViewRenderingMember
```

### ViewRenderingMember

produces [ViewRenderingMembership](../metamodel/elements/ViewRenderingMembership.md) · clause `8.2.2.26.1` · features `ownedRelatedElement +=`

```kebnf
ViewRenderingMember : ViewRenderingMembership =
    MemberPrefix 'render'
    ownedRelatedElement += ViewRenderingUsage
```

### ViewRenderingUsage

produces [RenderingUsage](../metamodel/elements/RenderingUsage.md) · clause `8.2.2.26.1` · features `ownedRelationship +=`

```kebnf
ViewRenderingUsage : RenderingUsage =
      ownedRelationship += OwnedReferenceSubsetting
      FeatureSpecializationPart?
      UsageBody
    | ( UsageExtensionKeyword* 'rendering'
      | UsageExtensionKeyword+ )
      Usage
```

## 8.2.2.26.2 View Usages

### ViewUsage

clause `8.2.2.26.2`

```kebnf
ViewUsage =
    OccurrenceUsagePrefix 'view'
    UsageDeclaration? ValuePart?
    ViewBody
```

### ViewBody

produces [ViewUsage](../metamodel/elements/ViewUsage.md) · clause `8.2.2.26.2`

```kebnf
ViewBody : ViewUsage =
    ';' | '{' ViewBodyItem* '}'
```

### ViewBodyItem

produces [ViewUsage](../metamodel/elements/ViewUsage.md) · clause `8.2.2.26.2` · features `ownedRelationship +=`

```kebnf
ViewBodyItem : ViewUsage =
      DefinitionBodyItem
    | ownedRelationship += ElementFilterMember
    | ownedRelationship += ViewRenderingMember
    | ownedRelationship += Expose
```

### Expose

clause `8.2.2.26.2`

```kebnf
Expose =
    'expose' ( MembershipExpose | NamespaceExpose )
    RelationshipBody
```

### MembershipExpose

clause `8.2.2.26.2`

```kebnf
MembershipExpose =
    MembershipImport
```

### NamespaceExpose

clause `8.2.2.26.2`

```kebnf
NamespaceExpose =
    NamespaceImport
```

## 8.2.2.26.3 Viewpoints

### ViewpointDefinition

clause `8.2.2.26.3`

```kebnf
ViewpointDefinition =
    OccurrenceDefinitionPrefix 'viewpoint' 'def'
    DefinitionDeclaration RequirementBody
```

### ViewpointUsage

clause `8.2.2.26.3`

```kebnf
ViewpointUsage =
    OccurrenceUsagePrefix 'viewpoint'
    ConstraintUsageDeclaration RequirementBody
```

## 8.2.2.26.4 Renderings

### RenderingDefinition

clause `8.2.2.26.4`

```kebnf
RenderingDefinition =
    OccurrenceDefinitionPrefix 'rendering' 'def'
    Definition
```

### RenderingUsage

clause `8.2.2.26.4`

```kebnf
RenderingUsage =
    OccurrenceUsagePrefix 'rendering'
    Usage
```

## 8.2.2.27 Metadata Textual Notation

### MetadataDefinition

clause `8.2.2.27` · features `isAbstract ?=`

```kebnf
MetadataDefinition =
    ( isAbstract ?= 'abstract')? DefinitionExtensionKeyword*
    'metadata' 'def' Definition
```

### PrefixMetadataAnnotation

produces [Annotation](../metamodel/elements/Annotation.md) · clause `8.2.2.27` · features `annotatingElement =`, `ownedRelatedElement +=`

```kebnf
PrefixMetadataAnnotation : Annotation =
    '#' annotatingElement = PrefixMetadataUsage
    { ownedRelatedElement += annotatingElement }
```

### PrefixMetadataMember

produces [OwningMembership](../metamodel/elements/OwningMembership.md) · clause `8.2.2.27` · features `ownedRelatedElement =`

```kebnf
PrefixMetadataMember : OwningMembership =
    '#' ownedRelatedElement = PrefixMetadataUsage
```

### PrefixMetadataUsage

produces [MetadataUsage](../metamodel/elements/MetadataUsage.md) · clause `8.2.2.27` · features `ownedRelationship +=`

```kebnf
PrefixMetadataUsage : MetadataUsage =
    ownedRelationship += OwnedFeatureTyping
```

### MetadataUsage

clause `8.2.2.27` · features `ownedRelationship +=`

```kebnf
MetadataUsage =
    UsageExtensionKeyword* ( '@' | 'metadata' )
    MetadataUsageDeclaration
    ( 'about' ownedRelationship += Annotation
      ( ',' ownedRelationship += Annotation )*
    )?
    MetadataBody
```

### MetadataUsageDeclaration

produces [MetadataUsage](../metamodel/elements/MetadataUsage.md) · clause `8.2.2.27` · features `ownedRelationship +=`

```kebnf
MetadataUsageDeclaration : MetadataUsage =
    ( Identification ( ':' | 'typed' 'by' ) )?
    ownedRelationship += OwnedFeatureTyping
```

### MetadataBody

produces [Type](../metamodel/elements/Type.md) · clause `8.2.2.27` · features `ownedRelationship +=`

```kebnf
MetadataBody : Type =
    ';' |
    '{' ( ownedRelationship += DefinitionMember
        | ownedRelationship += MetadataBodyUsageMember
        | ownedRelationship += AliasMember
        | ownedRelationship += Import
        )*
    '}'
```

### MetadataBodyUsageMember

produces [FeatureMembership](../metamodel/elements/FeatureMembership.md) · clause `8.2.2.27` · features `ownedMemberFeature =`

```kebnf
MetadataBodyUsageMember : FeatureMembership =
    ownedMemberFeature = MetadataBodyUsage
```

### MetadataBodyUsage

produces [ReferenceUsage](../metamodel/elements/ReferenceUsage.md) · clause `8.2.2.27` · features `ownedRelationship +=`

```kebnf
MetadataBodyUsage : ReferenceUsage =
    'ref'? ( ':>>' | 'redefines' )? ownedRelationship += OwnedRedefinition
    FeatureSpecializationPart? ValuePart?
    MetadataBody
```

### ExtendedDefinition

produces [Definition](../metamodel/elements/Definition.md) · clause `8.2.2.27`

```kebnf
ExtendedDefinition : Definition =
    BasicDefinitionPrefix? DefinitionExtensionKeyword+
    'def' Definition
```

### ExtendedUsage

produces [Usage](../metamodel/elements/Usage.md) · clause `8.2.2.27`

```kebnf
ExtendedUsage : Usage =
    UnextendedUsagePrefix UsageExtensionKeyword+
    Usage

// End of BNF
```
